# 9.UX.4 audit — Org hierarchy CHECK + inheritance-hint propagation

Date: 2026-04-30
Lane: `compassionate-mayer-b9eac6`

## Gap 1 — DB CHECK constraint test

The migration `db/migrations/202604290101_phase_9_hierarchy_access_wiring.sql`
locked `user_roles_scope_payload_check` so no row can carry both
`location_id` AND `org_unit_id` non-null regardless of `scope_type`. The
gap was the absence of a focused test pinning that contract.

- File: [test/db_constraints/user_roles_scope_check_test.dart](../../test/db_constraints/user_roles_scope_check_test.dart)
- Test names:
  - `user_roles scope-payload CHECK migration shape declares user_roles_scope_payload_check`
  - `user_roles scope-payload CHECK migration shape rejects both location_id and org_unit_id non-null for every scope_type`
  - `user_roles scope-payload CHECK integration (passive default)` — env-gated INSERT probe; skips without `FORGE_FLOW_RUN_STAGING_HIERARCHY_TEST=true`
- Outcome: 2 passed, 1 skipped (passive-by-default integration layer
  matches the `phase_9_hierarchy_access_wiring_test.dart` pattern).

## Gap 2 — `source_org_unit_id` propagation trace

Traced the field across the four expected layers between Postgres
and the Settings → Team → Org Hierarchy widget.

| Layer | File:line | Finding |
| --- | --- | --- |
| Postgres column | [db/migrations/202604290101_phase_9_hierarchy_access_wiring.sql:220](../../db/migrations/202604290101_phase_9_hierarchy_access_wiring.sql) | `source_org_unit_id uuid null` exists on `user_effective_locations` and is populated by `refresh_user_effective_locations` (line 288). |
| Repository read | [lib/infrastructure/persistence/postgres/repositories/user_roles_repository.dart:344](../../lib/infrastructure/persistence/postgres/repositories/user_roles_repository.dart) | `_selectColumns` projects `uel.location_id::text` into `effective_location_ids` only. `source_org_unit_id` is NOT pulled back. `UserRoleGrantRow` has no `sourceOrgUnitId` field. |
| Gateway DTO | [lib/services/auth/auth_operations_gateway.dart:318](../../lib/services/auth/auth_operations_gateway.dart) | No DTO returns `user_effective_locations` rows. `TeamOrgHierarchyListed` carries `TeamOrgUnitEntry` + `TeamOrgLocationEntry`, both populated from `org_units` / `locations` directly (see [repository_auth_operations_gateway.dart:610](../../lib/services/auth/repository_auth_operations_gateway.dart)), not from `user_effective_locations`. |
| Widget (in-lane) | [lib/screens/settings/settings_org_hierarchy_section.dart:399](../../lib/screens/settings/settings_org_hierarchy_section.dart) | `_AddChildOrgUnitDialog` already renders the parent unit name via `widget.parent.label` — the parent `TeamOrgUnitEntry` is the source-of-inheritance analog the section owns at this layer. Read path is correct; no field is dropped on the widget side. |

Change applied: **none**. The widget already conformantly renders the
parent unit name via the existing `TeamOrgUnitEntry.label` propagation
that flows through `listOrgHierarchy`. Restoring `source_org_unit_id`
all the way to a grant-aware DTO would require new shape on
`auth_operations_gateway.dart` (Block 2 hard-constraint: do not touch),
new fields on `proxy_auth_operations_gateway.dart` (also blocked), and
a new render path in `team_settings_section.dart`'s role-change dialog
(Block 2: do not redesign). All those changes are out of lane scope —
flagged here so a follow-up slice can pick up the gateway side under
the right ownership.

- Widget test: [test/settings_org_hierarchy_inheritance_hint_test.dart](../../test/settings_org_hierarchy_inheritance_hint_test.dart)
  - `Add-child dialog renders the parent unit name when the parent corresponds to a non-null source org unit`
  - `Add-child dialog still renders the parent unit name when the parent is the operator root (corp-level source unit)`
- Outcome: 2/2 passed.

## Verification commands

- `dart analyze` — clean (no issues found across the project).
- `flutter test test/db_constraints/user_roles_scope_check_test.dart` — 2 passed, 1 skipped.
- `flutter test test/settings_org_hierarchy_inheritance_hint_test.dart` — 2 passed.
