# 8.S.PU Walkthrough — Push Operations scheduling adapter (poll-only + bearer keyPaste, lifecycle = `documented`)

**Slice:** 8.S.PU (Phase 8.S / Wave B — engineer-all-17 push, scheduling
poll-only + bearer keyPaste variant)
**Mode:** `kDemoMode = true` (operator app, deterministic seed). No
live HTTP — engineering slice.
**Date:** 2026-05-04
**Demo start condition:**
- Business date `2026-W18 Mon` (Monday of ISO week 18, 2026).
- Operator: "Demo Diner Co.", Location: "Toronto Yorkville".
- Shift dashboard scope: whole-day.
- All vendor connections empty. `demo_mode_state.is_demo = true` for
  `pos`, `labor`, `reservation`.

This walkthrough is a numbered click-path per the
`docs/CODEX_PROMPT_GENERATION_STANDARD.md` Walkthrough Specificity
section. The slice ships at lifecycle = `documented` — the picker
shows Push Operations as **Coming soon** with no Connect button. Live
HTTP is the responsibility of `8.S.PU.live.sandbox` and
`8.S.PU.live.prod`. The engineering anchors below verify that the
adapter framework calls (sanity hook, idempotency, watermark,
key-paste connect, disconnect/reconnect) all satisfy the Vendor
Adapter Slice Contract on the documented path. The poll-only nature
replaces the forged-signature anchor with an explicit "webhooks N/A
— pollOnly" acknowledgement; the bearer-token keyPaste auth replaces
the OAuth near-expiry anchor with a "no refresh hop — partner-issued
static credential" acknowledgement.

## Click-path

### 1. Empty state — vendor picker shows Push Operations "Coming soon"

- Operator opens the app on `2026-05-04 (Mon, W18)`. Settings →
  Vendor connections.
- Scheduling card section is empty. Card slot reads:
  > **Connect your scheduling system**
  > Forge & Flow needs to read shift schedules and time-clock data
  > to power CPLH, SPLH and your wage chrome. Pick yours and we walk
  > you through connecting it.
  > [Choose your scheduling vendor]
- Tap **Choose your scheduling vendor** → vendor picker dialog
  (`vendor_connections_picker_dialog`). Dropdown lists every Wave B
  scheduling vendor.
- Push Operations entry:
  - **Widget under test:** `Key('vendor_picker_push_operations')`.
  - **Expected label:** "Push Operations".
  - **Expected secondary chrome:** "Coming soon" pill with subtitle
    "Available once we finish the Push Operations partner approval —
    usually 4-6 weeks." (matches `partnership_status.md` lead-time
    estimate.)
  - **Expected button state:** Connect button is HIDDEN; replaced
    with "Notify me when ready" button (deferred behavior — taps
    open the email-capture sheet that lands as part of the 11W
    vendor-picker slice; the picker itself surfaces the lifecycle
    today).
- Tap **Cancel** → dialog dismisses. Scheduling card section unchanged.

### 2. Engineering anchor — testConnection sample shape

- (Engineering slice anchor; not operator-visible at lifecycle =
  `documented`. Verified by
  `test/integrations/labor/push_operations_labor_adapter_test.dart`
  group `testConnection`.)
- Adapter is constructed with the fixture sample
  `samplePushOperationsShift` (`id: 800101`, `employee_id: 5512`,
  `position_name: 'Server'`, `start_at: 2026-05-02T16:00:00.000Z`,
  `end_at: 2026-05-02T23:30:00.000Z`,
  `updated_at: 2026-05-02T15:45:00.000Z`).
- Calling `adapter.testConnection(...)` returns:
  - `authValid: true`.
  - `fieldMapping`:
    - `shift_start: 2026-05-02 16:00:00 UTC`.
    - `shift_end: 2026-05-02 23:30:00 UTC`.
    - `role_name: 'Server'`.
    - `employee_id: '5512'`.
    - `vendor_entity_id: '800101'`.
    - `covers_source: 'not_applicable'`.
  - `elapsedMs < 30 * 1000` (V1 lean cut 2 — no 5s SLA).

### 3. Engineering anchor — backfill watermark per batch + sanity-hook reject

- Engineering slice anchor verified by
  `test/integrations/labor/push_operations_labor_adapter_test.dart`
  group `backfill`.
- Adapter runs a 60-day backfill against a fake API client returning
  three records: `samplePushOperationsShift` (good),
  `futureDatedPushOperationsShift` (sanity hook returns `false`),
  `secondPushOperationsShift` (good).
- Expected outcome:
  - `command.sanityHook` is called once per record (3 calls);
    `vendorEventId` arguments are `'800101'`, `'999001'`, `'800102'`
    in order.
  - The future-dated record (`'999001'`) is dropped — no canonical
    write — because the hook returns `false`. Framework already wrote
    `sanity_log` + `connector_sync_log 'sanity_drop'`; the adapter
    skips the canonical write.
  - The other two records are upserted via
    `PushOperationsCanonicalSink.upsertShift`.
  - `BackfillResult.recordsWritten == 2`; `BackfillResult.completed
    == true`.
  - **Expected watermark advance(s):** at least one
    `advanceWatermark` call after the batch commit; the persisted
    `cursor_token` matches the page's `nextCursor` (empty string when
    the page chain is exhausted).

### 4. Engineering anchor — polling tick reads watermark and resumes

- Engineering slice anchor verified by the same test group.
- After the backfill in step 3, `pollIncremental` is invoked with
  `lastModifiedSeen` set to the backfill's persisted value.
- The fake API client returns one new record on the next tick. The
  adapter:
  - Calls `command.sanityHook` once (returns `true`).
  - Upserts via `upsertShift`.
  - Calls `advanceWatermark` with the page's `nextCursor` (empty
    string here) and the new `lastModifiedSeen`.
- Expected `PollIncrementalResult.recordsWritten == 1`,
  `sanityDropped == 0`.

### 5. Engineering anchor — idempotency (same payload twice → 1 write)

- Engineering slice anchor verified by `pollIncremental` test
  re-running the same fixture page twice.
- The fake `PushOperationsCanonicalSink` keys on
  `(vendor_entity_id, vendor_modified_at)`; the second call returns
  `false` (no-op).
- **Expected widget:** none (server-side flow).
- **Expected sink state:** `upsertCalls == 2`; `uniqueKeys.length ==
  1`.

### 6. Engineering anchor — mid-batch crash resume from cursor

- Engineering slice anchor verified by the watermark test group.
- The fake API client throws on page 3 (`crashAfterPage = 2`). Pages
  1 and 2 deliver records; page 3 throws.
- Expected behavior:
  - Two `advanceWatermark` calls land before the throw (one per
    successful page).
  - The last persisted cursor is `'page:3'`; the last persisted
    `last_modified_seen` is the page-2 timestamp.
  - On resume (next worker invocation), the adapter is constructed
    again with `BackfillCommand.resumeFromCursor = 'page:3'`. The
    fake's reset sequence shows the adapter requests page chain
    starting from that cursor — verified by
    `apiClient.requestedCursors.contains('page:3')`.
  - No record is double-written because the canonical UNIQUE
    collapses any retry of pages 1-2.

### 7. Engineering anchor — handleWebhook throws (poll-only)

- Engineering slice anchor verified by
  `test/integrations/labor/push_operations_labor_adapter_test.dart`
  group `handleWebhook`.
- Calling `adapter.handleWebhook(...)` with any payload throws
  `UnsupportedError('Push Operations does not support webhooks;
  pollOnly')`.
- The framework's `InboundWebhookHandler.dispatch` consults
  `capabilityProfile.webhookSupport` BEFORE dispatch and never
  delivers a webhook to a poll-only adapter; the throw is the
  defense-in-depth signal that catches a future router refactor that
  forgets the gate.
- **Webhooks N/A — pollOnly path; signature reject test skipped.**
  See `webhook_signature.md` (single line N/A) and
  `live_verification_checklist.md` row "Webhook signature
  verification" (checked off ✅ N/A in advance).

### 8. Engineering anchor — bearer keyPaste connect (no OAuth refresh hop)

- Engineering slice anchor; lives at the connect-flow seam rather than
  in `oauth_refresh_cron.dart` because Push Operations issues a
  partner-issued static bearer token with no refresh-token rotation.
- The admin connect dialog (`vendor_connect_dialog`) renders a
  bearer-paste field for Push Operations (`authMode = keyPaste`); the
  operator pastes the partner-issued bearer and taps **Connect**.
- The framework wraps the pasted bearer in a
  `VendorCredentialHandle` (issued by
  `vendor_credentials_repository.dart`); plaintext never reaches the
  adapter.
- `adapter.connect(...)` returns:
  - `status: ConnectionStatus.connected`.
  - `metadata.grant_scope: 'operatorWide'` (one bearer covers every
    location bound to the Push company).
  - `metadata.api_version: 'v1'`.
  - `webhookUrl: null` (poll-only).
  - `firstBackfillStarted: true`.
- **No OAuth near-expiry refresh.** Push bearer is durable; `oauth_refresh_cron.dart`
  is a no-op for `vendorId == 'push_operations'`. Token revocation at
  the partner portal surfaces as 401 on the next adapter call →
  connection flips to `error` after three consecutive failures (same
  3-strike path the OAuth vendors use). **No email_outbox row at
  V1** (deferred to `9.8.email`).

### 9. Engineering anchor — disconnect → reconnect watermark preservation

- Engineering slice anchor verified by the connect/disconnect smoke
  test group.
- `adapter.disconnect(...)` returns `(credentialsWiped: true,
  webhookUnregistered: true, watermarkPreserved: true)`.
- `connector_sync_log` row: `event_kind = 'disconnect'` with
  `payload_preview` carrying the reason + actor.
- After disconnect, the watermark row in `connector_sync_watermark`
  for `(operator, location, vendor)` is preserved (not deleted). On
  reconnect, `pollIncremental` is invoked with `lastModifiedSeen` set
  to the persisted watermark; the fake empty-page response confirms
  no record is double-written or skipped.
- **Expected sink state:** `watermarkAdvances` is non-empty across
  the disconnect call.

### 10. Adversarial — future-dated shift (sanity drop)

- The polling tick fetches a record with `start_at = now() + 5 days`.
- The framework's `sanityHook` (bound in
  `tool/integration_sync_worker/integration_sync_worker.dart`) calls
  `VendorTimestampSanity.evaluate` and writes a `sanity_log` row
  with `rule = 'opened_in_future'` (the labor-side equivalent flag
  for shift_start). The hook returns `false`.
- Adapter behavior:
  - Skips the canonical write for `'999001'`.
  - Increments `PollIncrementalResult.sanityDropped`.
  - Continues processing the remaining records.

### 11. Adversarial — webhook delivery attempt (poll-only)

- A bad actor (or vendor misconfiguration) sends a forged HTTP request
  to `/v1/webhooks/push_operations/<operator_id>/<location_id>`.
- The proxy resolves `vendorId = 'push_operations'` and consults the
  registered adapter map. **Forged-signature step replaced by:** the
  framework router reads
  `adapter.capabilityProfile.webhookSupport == VendorWebhookSupport.pollOnly`
  and returns `404` with `WebhookOutcome.vendorNotFound`-equivalent
  semantics — the adapter never receives the request, and
  `handleWebhook` is not invoked. No canonical fact is written.
- This anchor replaces the per-vendor signature-reject test for
  signing vendors. See `webhook_signature.md` (N/A line) and the
  `handleWebhook` test that asserts the throw as defense-in-depth.

## Acceptance evidence

| Check | Result |
|---|---|
| `dart analyze` | clean across new files |
| `flutter test test/integrations/labor/push_operations_labor_adapter_test.dart` | framework calls covered (sanity hook, idempotency, watermark, handleWebhook throw, smoke, testConnection, capability profile); banned-items grep clean |
| Capability profile assertions | lifecycle = `documented`; webhookSupport = pollOnly; coversFieldExposed = false; authMode = keyPaste; grantScope = operatorWide; category = labor |
| Doc pack | 6 files populated at `docs/integrations/push_operations/`; `webhook_signature.md` is the single-line N/A; `oauth_shape.md` is the N/A bearer-token line; `partnership_status.md` records `not_started` + 4-6 weeks |
| Walkthrough specificity | numbered steps; widgets named (`Key('vendor_picker_push_operations')`); values named (`shift_start: 2026-05-02 16:00:00 UTC`, `id: 800101`, `position_name: 'Server'`); pollOnly + bearer-keyPaste anchors named |

## Documented follow-ups

- `8.S.PU.live.sandbox`: wire the real HTTP client into the adapter,
  paste the partner-issued sandbox bearer token (via
  `VendorCredentialHandle`), run the live verification checklist
  (every row except the pre-checked webhook N/A row).
- `8.S.PU.live.prod`: re-run the checklist against production
  bearer issued via Push Operations partner approval; promote
  lifecycle to `production_credentialed`.
- `8.S.PU.live.*` extension: wire `/api/v1/labour` (date-range with
  2-day max window) for punch-level `clocked_in` / `clocked_out` /
  breaks canonical facts. The `documented` slice ships shifts only;
  labour entries are a follow-up canonical fact path.
- 11W vendor-picker "Notify me when ready" email capture (deferred
  surface; covered by the 11W picker slice).
