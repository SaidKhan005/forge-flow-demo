# 8.0.lifecycle Walkthrough — Engineering shell verification

**Slice:** 8.0.lifecycle (Phase 8 — Wave B framework micro-amendment)
**Mode:** Engineering shell only. This slice ships data shape + a new
table; the operator-facing chrome ("Coming soon" pill, "Notify me when
ready" capture) lands when the picker widget is mounted operator-side
in `11W.8`. So the click-path here is engineering-shell evidence
(analyzer + tests + migration scanner + grep), not in-app.
**Date:** 2026-05-04
**Demo start condition:**
- Worktree: `.claude/worktrees/magical-hoover-a5eb08`.
- Branch: `claude/magical-hoover-a5eb08` off master `4c1b2d8` (the
  parent commit that locked the engineer-all-17 doctrine).
- `dart pub get` already ran clean against the worktree.

This walkthrough is a numbered click-path per the
`docs/CODEX_PROMPT_GENERATION_STANDARD.md` Walkthrough Specificity
section. Each step names the command, the expected output line, and
the named symbol / file / column the step locks down.

The slice realizes one item from the engineer-all-17 doctrine
(`memory/project_phase_8_engineer_all_17_doctrine.md`):

> The lifecycle field lives on `VendorCapabilityProfile`. Slice
> `8.0.lifecycle` extends the existing boolean `partnershipGated` to
> a `lifecycle: VendorLifecycle` enum and is the first item in
> Wave B. Every other adapter lane consumes this lane after merge.

And it ships the data the Operator Web vendor picker writes into when
an admin taps **Notify me** on a `documented` / `sandbox_verified`
row, per `docs/phases/phase_8/vendor_connections_admin_surface.md`
("Backend UX exposure" → "Notify me when ready").

## Click-path

### 1. `dart analyze` — primary scope

- **Command:** `dart analyze lib/services/integration/integration_adapter_common.dart test/services/integration/integration_adapter_common_test.dart test/db/migrations/phase_8_0_lifecycle_migration_test.dart`
- **Expected last line:** `No issues found!`
- **What this proves:** the new `VendorLifecycle` enum + `lifecycle`
  field on `VendorCapabilityProfile` parse cleanly together with the
  two new test files. The doc-comment angle-bracket lint
  (`unintended_html_in_doc_comment`) on
  `lib/services/integration/integration_adapter_common.dart:54` is
  resolved by wrapping `<N>` in backticks.
- **Symbols under test:** `VendorLifecycle.documented`,
  `VendorLifecycle.sandboxVerified`,
  `VendorLifecycle.productionCredentialed`,
  `VendorLifecycle.liveWithOperators`,
  `VendorCapabilityProfile.lifecycle`.

### 2. `dart analyze` — picker UI + handler-test consumers

- **Command:** `dart analyze lib/integrations/ui/vendor_connections/ test/integration/inbound_webhook_handler_test.dart`
- **Expected last line:** `No issues found!`
- **What this proves:** the picker UI (`vendor_connections_models.dart`
  re-export, `_VendorPickerDialogState._labelFor` lifecycle switch,
  `InMemoryVendorConnectionsGateway._vendorCatalog` const list) and
  the inbound-webhook-handler test stub (`_StubPosAdapter.capabilityProfile`)
  all flipped from `partnershipGated: bool` to `lifecycle:
  VendorLifecycle` cleanly.

### 3. Migration drift scanner

- **Command:** `dart run tool/migration_drift_scanner.dart --fix --strict-docs`
- **Expected line about cutoff:**
  `migration_drift_scanner: latest=202605040400_phase_8_0_lifecycle_add_vendor_lifecycle_notification.sql; cutoff=202605040400_phase_8_0_lifecycle_add_vendor_lifecycle_notification.sql; report=build/reports/migration_drift_report.md`
- **Expected line about staging setup:**
  `migration_drift_scanner: updated scripts/postgres_staging_setup.ps1`
- **Expected line about authority docs:**
  `migration_drift_scanner: report contains stale authority docs`
  — this is **expected** for a brand-new migration. The stale rows
  in `build/reports/migration_drift_report.md` are
  `PROJECT_TRACKER.md`, `docs/POST_HARDENING_FOLLOWUPS.md`,
  `runbooks/phase_9_production1_migration_apply_runbook.md`,
  `docs/phases/phase_9/phase_9_execution_backlog.md`,
  `docs/phases/phase_11A_operations_console/phase_11A_operations_console_plan.md`.
  Per the Block 2 prompt these stay frozen this slice; Codex updates
  them post-ACCEPT.
- **What this proves:** the migration filename sorts after the prior
  cutoff and the staging-setup runbook is rewritten to point at the
  new latest. The `--strict-docs` exit code 1 is acknowledged in the
  execution report.

### 4. Migration cutoff lint

- **Command:** `dart run tool/migration_cutoff_lint.dart`
- **Expected last line:** `migration_cutoff_lint: clean — runbook cutoff is current.`
- **Expected count line:** `migration_cutoff_lint: scanned 66 migration(s); cutoff=202605040400_phase_8_0_lifecycle_add_vendor_lifecycle_notification.sql.`
- **What this proves:** the runbook cutoff banner in
  `scripts/postgres_staging_setup.ps1` between
  `# MIGRATION_CUTOFF_BEGIN` and `# MIGRATION_CUTOFF_END` lines up
  with the latest migration filename on disk.

### 5. `flutter test` on the two new test files

- **Command:** `flutter test test/services/integration/integration_adapter_common_test.dart test/db/migrations/phase_8_0_lifecycle_migration_test.dart`
- **Expected last line:** `00:00 +21: All tests passed!`
- **Expected progression:** 21 tests, three groups
  (`VendorLifecycle enum` ×3,
  `VendorCapabilityProfile` ×3,
  `vendor_lifecycle_notification table` ×4 +
  `UNIQUE constraint + indexes` ×3 +
  `RLS policy stub` ×4 +
  `Grants` ×2 +
  `Migration hygiene` ×2).
- **What this proves:** the enum order is locked (`documented` is
  index 0, four values total), the enum value names round-trip as
  stable string literals (`name == 'documented'`,
  `name == 'sandboxVerified'`, etc.), the migration declares every
  binding column (`notification_id`, `operator_id`, `vendor_id`,
  `email`, `requested_at`, `notified_at`), the UNIQUE on
  `(operator_id, vendor_id, email)` is present and lead-column-correct,
  the RLS policy uses the `app_current_operator()` wrapper instead
  of bare `current_setting()`, and the migration is wrapped in
  `begin;` / `commit;`.

### 6. Repo-wide `partnershipGated` grep

- **Command:** `grep -rn "partnershipGated" lib/ tool/ test/`
- **Expected:** no output, exit code 1 (no matches found).
- **What this proves:** the old boolean has been removed cleanly —
  no compatibility shim, no commented-out reference, no stale test
  assertion. The picker UI now reads `lifecycle` directly. The
  Wave B adapter lanes that build on top of this slice will declare
  `lifecycle: VendorLifecycle.documented` on every
  `VendorCapabilityProfile` they construct.
- **Out-of-scope hits in markdown:** `PROJECT_TRACKER.md:137`,
  `docs/contracts/vendor_adapter_slice_contract.md:61`,
  `docs/phases/phase_8/vendor_master_list.md:180`,
  `docs/phases/phase_8/phase_8_live_pos_labor_adapter_plan.md:34`
  — these are the slice's own forward-looking descriptions; per the
  Block 2 "no tracker / phase-doc / contract edits" rule, Codex
  updates them post-ACCEPT.
