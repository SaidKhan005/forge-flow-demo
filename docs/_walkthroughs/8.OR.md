# 8.OR Walkthrough — Oracle MICROS Simphony POS adapter (poll-only, lifecycle = `documented`)

**Slice:** 8.OR (Phase 8 / Wave B — engineer-all-17 push, poll-only POS variant)
**Mode:** `kDemoMode = true` (operator app, deterministic seed). No live HTTP — engineering slice.
**Date:** 2026-05-04
**Demo start condition:**
- Business date `2026-W18 Mon` (Monday of ISO week 18, 2026).
- Operator: "Demo Diner Co.", Location: "Toronto Yorkville".
- Shift dashboard scope: whole-day.
- All vendor connections empty. `demo_mode_state.is_demo = true` for `pos`, `labor`, `reservation`.

This walkthrough is a numbered click-path per the
`docs/CODEX_PROMPT_GENERATION_STANDARD.md` Walkthrough Specificity
section. The slice ships at lifecycle = `documented` — the picker
shows Oracle MICROS Simphony as **Coming soon** with no Connect
button. Live HTTP is the responsibility of `8.OR.live.sandbox` and
`8.OR.live.prod`. The engineering anchors below verify that the
adapter framework calls (sanity hook, idempotency, watermark, OAuth
refresh, disconnect/reconnect) all satisfy the Vendor Adapter Slice
Contract on the documented path. The poll-only nature replaces the
forged-signature anchor with an explicit "webhooks N/A — pollOnly"
acknowledgement.

## Click-path

### 1. Empty state — vendor picker shows Oracle MICROS Simphony "Coming soon"

- Operator opens the app on `2026-05-04 (Mon, W18)`. Settings →
  Vendor connections.
- POS card section is empty. Card slot reads:
  > **Connect your Point-of-sale (POS) system**
  > Forge & Flow needs to read sales and order data from your POS to
  > power your dashboard, baseline math, and Shift live view. Pick
  > yours and we walk you through connecting it.
  > [Choose your POS]
- Tap **Choose your POS** → vendor picker dialog
  (`vendor_connections_picker_dialog`). Dropdown lists every Wave B
  vendor.
- Oracle MICROS Simphony entry:
  - **Widget under test:** `Key('vendor_picker_oracle_micros_simphony')`.
  - **Expected label:** "Oracle MICROS Simphony".
  - **Expected secondary chrome:** "Coming soon" pill with subtitle
    "Available once we finish the Simphony Partner Integration
    Program — usually 8-16 weeks." (matches
    `partnership_status.md` lead-time estimate.)
  - **Expected button state:** Connect button is HIDDEN; replaced
    with "Notify me when ready" button (deferred behavior — taps
    open the email-capture sheet that lands as part of the
    11W vendor-picker slice; the picker itself surfaces the lifecycle
    today).
- Tap **Cancel** → dialog dismisses. POS card section unchanged.

### 2. Engineering anchor — testConnection sample shape

- (Engineering slice anchor; not operator-visible at lifecycle =
  `documented`. Verified by
  `test/integrations/pos/oracle_micros_simphony_pos_adapter_test.dart`
  group `testConnection`.)
- Adapter is constructed with the fixture sample
  `sampleSimphonyGuestCheck` (`chkNum: 412901`, `numOfGst: 5`,
  `opnUTC: 2026-05-02T22:45:00.000Z`, `clsdUTC:
  2026-05-02T23:32:14.000Z`, `subTtlCents: 12485`).
- Calling `adapter.testConnection(...)` returns:
  - `authValid: true`.
  - `fieldMapping`:
    - `covers: 5`.
    - `opened_at: 2026-05-02 22:45:00 UTC`.
    - `closed_at: 2026-05-02 23:32:14 UTC`.
    - `actual_sales: 124.85`.
    - `vendor_entity_id: '412901'`.
    - `covers_source: 'direct'`.
  - `elapsedMs < 30 * 1000` (V1 lean cut 2 — no 5s SLA).

### 3. Engineering anchor — backfill watermark per batch + sanity-hook reject

- Engineering slice anchor verified by
  `test/integrations/pos/oracle_micros_simphony_pos_adapter_test.dart`
  group `backfill`.
- Adapter runs a 60-day backfill against a fake API client returning
  three records: `sampleSimphonyGuestCheck` (good),
  `futureDatedSimphonyGuestCheck` (sanity hook returns `false`),
  `secondSimphonyGuestCheck` (good).
- Expected outcome:
  - `command.sanityHook` is called once per record (3 calls);
    `vendorEventId` arguments are `'412901'`, `'999001'`, `'412902'`
    in order.
  - The future-dated record (`'999001'`) is dropped — no canonical
    write — because the hook returns `false`. Framework already wrote
    `sanity_log` + `connector_sync_log 'sanity_drop'`; the adapter
    skips the canonical write.
  - The other two records are upserted via
    `OracleMicrosSimphonyCanonicalSink.upsertGuestCheck`.
  - `BackfillResult.recordsWritten == 2`; `BackfillResult.completed
    == true`.
  - **Expected watermark advance(s):** at least one
    `advanceWatermark` call after the batch commit; the persisted
    `cursor_token` matches the page's `nextCursor` (empty string when
    the page chain is exhausted).

### 4. Engineering anchor — polling tick reads watermark and resumes

- Engineering slice anchor verified by the same test group.
- After the backfill in step 3, `pollIncremental` is invoked with
  `lastModifiedSeen` set to the backfill's persisted value.
- The fake API client returns one new record on the next tick. The
  adapter:
  - Calls `command.sanityHook` once (returns `true`).
  - Upserts via `upsertGuestCheck`.
  - Calls `advanceWatermark` with the page's `nextCursor` (empty
    string here) and the new `lastModifiedSeen`.
- Expected `PollIncrementalResult.recordsWritten == 1`,
  `sanityDropped == 0`.

### 5. Engineering anchor — idempotency (same payload twice → 1 write)

- Engineering slice anchor verified by `pollIncremental` test
  re-running the same fixture page twice.
- The fake `OracleMicrosSimphonyCanonicalSink` keys on
  `(vendor_entity_id, vendor_modified_at)`; the second call returns
  `false` (no-op).
- **Expected widget:** none (server-side flow).
- **Expected sink state:** `upsertCalls == 2`; `uniqueKeys.length ==
  1`.

### 6. Engineering anchor — mid-batch crash resume from cursor

- Engineering slice anchor verified by the watermark test group.
- The fake API client throws on page 3 (`crashAfterPage = 2`). Pages
  1 and 2 deliver records; page 3 throws.
- Expected behavior:
  - Two `advanceWatermark` calls land before the throw (one per
    successful page).
  - The last persisted cursor is `'cursor-after-batch-2'`; the last
    persisted `last_modified_seen` is the page-2 timestamp.
  - On resume (next worker invocation), the adapter is constructed
    again with `BackfillCommand.resumeFromCursor =
    'cursor-after-batch-2'`. The fake's reset sequence shows the
    adapter requests page chain starting from that cursor — verified
    by `apiClient.requestedCursors.contains('cursor-after-batch-2')`.
  - No record is double-written because the canonical UNIQUE
    collapses any retry of pages 1-2.

### 7. Engineering anchor — handleWebhook throws (poll-only)

- Engineering slice anchor verified by
  `test/integrations/pos/oracle_micros_simphony_pos_adapter_test.dart`
  group `handleWebhook`.
- Calling `adapter.handleWebhook(...)` with any payload throws
  `UnsupportedError('Oracle MICROS Simphony does not support
  webhooks; pollOnly')`.
- The framework's `InboundWebhookHandler.dispatch` consults
  `capabilityProfile.webhookSupport` BEFORE dispatch and never
  delivers a webhook to a poll-only adapter; the throw is the
  defense-in-depth signal that catches a future router refactor that
  forgets the gate.
- **Webhooks N/A — pollOnly path; signature reject test skipped.**
  See `webhook_signature.md` (single line N/A) and
  `live_verification_checklist.md` row "Webhook signature
  verification" (checked off ✅ N/A in advance).

### 8. Engineering anchor — OAuth near-expiry refresh via cron

- Engineering slice anchor verified by the existing
  `test/integration/oauth_refresh_cron_test.dart` (framework slice
  8.0; the adapter does not need its own copy — the cron refreshes
  every Wave B vendor's `vendor_credentials` via
  `oauth_refresh_cron.dart`).
- Time-travel the test clock so `vendor_credentials.token_expires_at
  = now() + 30 minutes` for the Simphony connection.
- The cron job runs `refresh_expiring_inbound_vendor_tokens()` and
  re-mints the access token via `POST /sim/api/v2/oauth/token`.
- Expected outcome:
  - `connector_sync_log` row: `event_kind = 'auth_refresh'`.
  - `vendor_credentials.consecutive_refresh_failures = 0`.
  - Connection stays `connected`.
  - **No advisory lock** (lean cut 2 trim).
  - **No email_outbox row at V1** (deferred to `9.8.email`).
- On three consecutive failures, `connector_connection.status` flips
  to `error`; admin widget surfaces the operator-facing copy "Please
  reconnect Oracle MICROS Simphony and sign back in."

### 9. Engineering anchor — disconnect → reconnect watermark preservation

- Engineering slice anchor verified by the connect/disconnect smoke
  test group.
- `adapter.disconnect(...)` returns `(credentialsWiped: true,
  webhookUnregistered: true, watermarkPreserved: true)`.
- `connector_sync_log` row: `event_kind = 'disconnect'` with
  `payload_preview` carrying the reason + actor.
- After disconnect, the watermark row in
  `connector_sync_watermark` for `(operator, location, vendor)` is
  preserved (not deleted). On reconnect, `pollIncremental` is invoked
  with `lastModifiedSeen` set to the persisted watermark; the fake
  empty-page response confirms no record is double-written or
  skipped.
- **Expected sink state:** `watermarkAdvances` is non-empty across
  the disconnect call.

### 10. Adversarial — future-dated guest check (sanity drop)

- The polling tick fetches a record with `opnUTC = now() + 5 days`.
- The framework's `sanityHook` (bound in
  `tool/integration_sync_worker/integration_sync_worker.dart`) calls
  `VendorTimestampSanity.evaluate` and writes a `sanity_log` row
  with `rule = 'opened_in_future'`. The hook returns `false`.
- Adapter behavior:
  - Skips the canonical write for `'999001'`.
  - Increments `PollIncrementalResult.sanityDropped`.
  - Continues processing the remaining records.

### 11. Adversarial — webhook delivery attempt (poll-only)

- A bad actor (or vendor misconfiguration) sends a forged HTTP request
  to `/v1/webhooks/oracle_micros_simphony/<operator_id>/<location_id>`.
- The proxy resolves `vendorId = 'oracle_micros_simphony'` and consults
  the registered adapter map. **Forged-signature step replaced by:**
  the framework router reads
  `adapter.capabilityProfile.webhookSupport == VendorWebhookSupport.pollOnly`
  and returns `404` with `WebhookOutcome.vendorNotFound`-equivalent
  semantics — the adapter never receives the request, and
  `handleWebhook` is not invoked. No canonical fact is written.
- This anchor replaces the per-vendor signature-reject test for
  signing vendors. See `webhook_signature.md` (N/A line) and the
  `handleWebhook` test that asserts the throw as defense-in-depth.

## Acceptance evidence

| Check | Result |
|---|---|
| `dart analyze` | clean across new files |
| `flutter test test/integrations/pos/oracle_micros_simphony_pos_adapter_test.dart` | 8/8 framework calls covered; banned-items grep clean |
| Capability profile assertions | lifecycle = `documented`; webhookSupport = pollOnly; coversFieldExposed = true; partnershipGated = true; grantScope = perLocation |
| Doc pack | 6 files populated at `docs/integrations/oracle_micros_simphony/`; `webhook_signature.md` is the single-line N/A |
| Walkthrough specificity | numbered steps; widgets named; values named (`covers: 5`, `actual_sales: 124.85`, `chkNum: 412901`); pollOnly anchors named |

## Documented follow-ups

- `8.OR.live.sandbox`: wire the real HTTP client into the adapter,
  exchange partner-issued sandbox `client_credentials` for an access
  token, run the live verification checklist (every row except the
  pre-checked webhook N/A row).
- `8.OR.live.prod`: re-run the checklist against production
  credentials issued via the Simphony Partner Integration Program;
  promote lifecycle to `production_credentialed`.
- 11W vendor-picker "Notify me when ready" email capture (deferred
  surface; covered by the 11W picker slice).
- `8.0.lifecycle` framework amendment: migrate `VendorLifecycle` from
  the adapter-local enum to `VendorCapabilityProfile.lifecycle`. The
  adapter's `lifecycle` getter becomes a pass-through.
