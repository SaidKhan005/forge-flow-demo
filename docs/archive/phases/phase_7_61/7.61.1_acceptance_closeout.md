# 7.61.1 Acceptance Closeout

Status: accepted 2026-05-03
Archive reason: resolved slice detail for the still-active Phase 7.61 family.

## Slice

`7.61.1` closed Finding F-1 from
`docs/phases/phase_7_61/phase_7_61_audit_plan.md`: the banned
`firstWhere(orElse: coversDown / ppaUp)` patterns in
`lib/services/history_teaching_analyzer.dart`.

## Accepted Behavior

- Leak summaries resolve through `LeverCards.lookup`.
- Unknown leak ids reset `mostCommonLeakId`, `mostCommonLeakCount`,
  `topLeakDayparts`, and `mostCommonLeakSideLabel` to explicit no-pattern
  values.
- Benchmark summaries filter to known catalog ids before both frequency and
  daypart aggregation, then keep a lookup guard as defense-in-depth.
- Unknown benchmark ids and mixed known/unknown benchmark inputs cannot leak
  phantom ids, counts, side labels, or dayparts into `LearnTeachingAnalyzer`.
- The F-2 empty-leak default (`mostCommonLeakId = 'covers_down'`) remains
  intentionally untouched for `7.61.2`.

## Evidence

- Walkthrough: `docs/_walkthroughs/7.61.1.md`
- Contract test: `flutter test test/contracts/phase_7_61_driver_key_test.dart`
  - accepted evidence: 28 active tests pass + 1 F-2 holdout skipped
- Analyzer regression tests:
  `flutter test test/history_teaching_analyzer_test.dart test/learn_teaching_analyzer_test.dart`
  - accepted evidence: 32/32 pass
- Review result: no findings after revision 2.

## Archive Decision

No active authority docs were moved to archive. The Phase 7.61 plan,
contract, and walkthrough index remain live until `7.61.2`, `7.61.3`, and the
deferred `7.61.4` disposition close. This file archives only the resolved
detail so `PROJECT_TRACKER.md` can stay concise.
