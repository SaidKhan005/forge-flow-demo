# Advisor Graph Activation — sub-plan (C1 + C3 + AGE-read)
Status: PAUSED 2026-05-27 (was ACTIVE, operator greenlit 2026-05-26). All waves are built + merged to master (G1/G2/G4/G4b/G5a + follow-ups F1/F2/F3); the lane is paused with the canonical phased resume plan at `advisor_graph_activation_resume_plan.md` (remaining = G5b advisor-reads-graph + go-live). Historical wave list follows. Waves: G1 both-brands candidate staging (SAFE, this PR); G2 C3 schema migration draft (op-gated, hold); G3 C1 un-pause + commit-batch proxy verify (op-gated, hold + deploy); G4 C3 semantic extraction tooling/AI (op-gated, hold + cost); G5 AGE rebuild + advisor reads graph (op-gated, hold + deploy). Operator decisions: BOTH brands in the graph (Barrio data only; Barrio app code stays paused), start C3 now in parallel, advisor reads the graph (AGE). All proxy/schema/canonical-write merges need explicit operator approval + a deploy + the go-live secret; paid AI-extraction runs need operator cost go-ahead.
