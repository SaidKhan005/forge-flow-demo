# Audit — Demo data Slice F: HP #11 scope-level overrides (timing/data-accuracy/wage) + variance-derived notifications

**Branch:** `claude/demo-slice-f-hp11-overrides-notifications` · **Base:** `master` @ `ff37202b`
**Date:** 2026-05-15 · **Pattern:** B (worker self-audit + independent re-read, file:line)
**Authority:** this prompt → `docs/_audits/per_daypart_v1/full_demo_data_spec.md` Slice F (§2c lines 140-144, §1.6 line 80, §1.7, Gap G9/G10) → `CLAUDE.md` HP #11 / HP #2 / HP #4.

## Scope delivered

One HP #11 override **per scope level**, deterministic, into the existing production tables — realized per the spec §4 watch item (mobile SQLite has no `org_units`/scope columns; a scope override = a per-`restaurant_id` row; "inherits" = ABSENCE of a row → reader falls back to the business default):

- **Business/operator default (inherited-FROM source):** Downtown (`DemoScope.restaurantId`) — existing `_seedDemoTimingConfig` (Mon week-start) + `_seedDemoWageRoleRows` (FOH 16.50 / BOH 21.35) + a new data-accuracy baseline (all 3 periods `vendor`). **Untouched by this slice** — it IS the baseline.
- **Region scope — East Region overrides timing:** North Loop gets a `restaurant_timing_configs` row with `week_start_day = Sunday` (≠ business default Monday).
- **District scope — Metro District overrides a data-accuracy covers-source:** North Loop gets a single `data_accuracy_service_period_settings_cache` `dinner` row `covers_source='manual'` (≠ baseline `vendor`); its lunch/late_night have no row → inherit.
- **Location scope — Riverside overrides wages; Harbour inherits:** Riverside gets `wage_role_rows` blending FOH 17.50 / BOH 22.35 (≠ business default); Harbour has **no override row in any of the 3 tables** → every reader resolves it to the inherited business default (the "inherited from Business" pill exercised end-to-end).
- **Notifications (§1.6 / G9):** one `app_notifications` row emitted from the worst **real** over-plan seeded `week_records` row (max positive `dollar_gap` = `WeekRecord.isOverModel`); the body's dollar figure IS that real seeded gap — Metric Honesty; no breach → no row.
- **Operator-web mirror:** `OperatorWebDemoWageAuthorityGateway` now defaults its seed to `kDemoWageRoleRowScopeFixture` (business default + Riverside override + Harbour/North-Loop inherit) so the web Wage authority surface tells the same scope story.

Files: `lib/infrastructure/persistence/sqlite/sqlite_database_seed.dart` (+390, one new contiguous Slice F region at EOF — 4 new private fns + helpers, zero deletions); `lib/infrastructure/persistence/sqlite/sqlite_database.dart` (+23, 2 call-site hunks in `reseedMockReplayForBusinessDate`); `lib/operator_web/services/operator_web_wage_authority_gateway.dart` (+126/-2, fixture + nullable `initial` default); `test/per_daypart_v1_demo_slice_f_hp11_overrides_notifications_test.dart` (new, 7 tests). `_seedWeeklyPlanSnapshotFromReplay` (#803), the per-location loop / `_DemoLocationProfile` / `_seedAdditionalLocationsFromReplay` (#807), `_buildDemoSeedCycle`, and any `demo_mode_state` hook (E pt2) — **untouched** (`git diff` grep for those symbols = empty).

## Pattern B — 14-lens

| # | Lens | Worker self-audit (file:line) | Independent re-read verdict |
|---|---|---|---|
| 1 | Slice intent — one override per scope level renders real inherited-vs-effective | Region: `_seedDemoScopeOverrideTimingConfig` writes North Loop week-start Sunday (`sqlite_database_seed.dart` Slice F region, `_seedDemoScopeOverrideTimingConfig`); District: `_seedDemoScopeOverrideDataAccuracy` writes Downtown 3×`vendor` baseline + North Loop dinner `manual`; Location: `_seedDemoScopeOverrideWageRows` writes Riverside FOH/BOH cohort. Test asserts distinct effective + correct inherited-source per level (`...slice_f...test.dart` Region/Location/District tests). | PASS — three reader paths verified: `RestaurantTimingConfigDao.getConfig` returns null when no row → caller inherits (`restaurant_timing_config_dao.dart:15`); `wage_role_rows` empty → `_weightedAvgFromRows` business-default fallback; `DataAccuracyServicePeriodSettingsCacheDao.getRows` empty → inherit. Harbour has zero rows in all three → fully inherited. |
| 2 | HP #11 — only ADD, never remove/break a scope/inherited/effective affordance | New region is append-only; Downtown business-default rows never written/rewritten by this slice; operator-web change adds a default fixture + nullable param (`operator_web_wage_authority_gateway.dart` ctor) — no affordance removed. | PASS — `git diff --stat` = +537/-2; the -2 is the ctor signature widening (`Iterable<…> initial = const []` → `Iterable<…>? initial`), backward compatible (router `OperatorWebDemoWageAuthorityGateway()` unchanged behavior modulo now-seeded fixture; explicit `const []` still respected). Inherited-source PILL for wage/data-accuracy is Gap 32/29 (gated UI per spec §1.7), documented in residuals — not removed here. |
| 2b | Metric Honesty — notification driven by a REAL breach | `_seedDemoVarianceBreachNotification` scans seeded `week_records`, picks max positive `dollar_gap`; emits only if `>0`; body interpolates the real `dollar_gap`/`actual_labor_pct`/`theoretical_labor_pct`. | PASS — test recomputes max gap independently from `week_records` and asserts the alert body contains exactly `$<round(maxGap)>` (fabrication would fail); no-breach branch asserts empty. |
| 3 | HP #2 — production tables only, no `demo_*`, no reader branch | Writes only `restaurant_timing_configs`, `wage_role_rows`, `data_accuracy_service_period_settings_cache`, `app_notifications` — all production. No new table; no `kDemoMode` branch; web side is fixture-into-existing-gateway (contract §"Acceptable patterns"). | PASS — `app_notification_service_test` (existing HP-area guard) green; readers untouched (`git diff` touches no repository/service/widget). |
| 4 | HP #4 per-(operator,location) isolation | Every insert keys a single demo `restaurant_id`; data-accuracy `location_id == restaurant_id`, `operator_id='n'` (mirrors `kDemoOperatorOperatorId`). | PASS — dedicated HP #4 test asserts Riverside wage absent under Downtown, North Loop timing absent under Downtown, and zero rows for a foreign `restaurant_id` across all 3 tables. |
| 5 | Determinism / NO RNG | All values compile-time constants or derived from deterministic `week_records`; every timestamp a fixed literal (`'2026-05-15T…'`, `'2020-01-01'`, `'<bizdate>T12:00:00.000Z'`) — never `DateTime.now()`; tie-break on `week_id`. | PASS — determinism test reseeds twice, fingerprints all override rows + the notification, asserts byte-identical. `reseedDemo` clears `app_notifications` then re-seeds the identical row. |
| 6 | Idempotency / advance / operator-authority safe | Each seeder skips a `restaurant_id` that already has a row (mirrors `_seedDemoWageRoleRows`) + `ConflictAlgorithm.ignore`; can never clobber operator-configured timing/wage/data-accuracy. | PASS — reseed (no delete of the 3 settings tables) → skip-if-present keeps rows stable; `app_notifications` cleared by `reseedDemo` then re-emitted (UNIQUE(restaurant_id,event_key) + ignore). |
| 7 | Concurrency — E pt2 / #803 / #807 untouched | New code is one contiguous region after `_ensureDemoRestaurant` (EOF); call sites are 2 isolated hunks in `reseedMockReplayForBusinessDate` only. | PASS — `git diff` symbol grep for `_seedWeeklyPlanSnapshotFromReplay`/`_buildDemoSeedCycle`/`_seedAdditionalLocationsFromReplay`/`_DemoLocationProfile`/`demo_mode_state` = empty; localized hunks, orchestrator rebases at merge. |
| 8 | Production path unchanged | Seeders are demo-`restaurant_id`-scoped and only invoked from the demo `reseedMockReplayForBusinessDate`; no proxy/sync/repository path altered. | PASS — `postgres_shift_record_to_mobile_sync` (the production data-accuracy cache writer) untouched; demo seed writes raw deterministic rows with `ignore`, never via the prod `replaceAll` (which would delete + use `now()`). |
| 9 | Placement rationale (reseed-only, mirrors Slice B) | Calls added in `reseedMockReplayForBusinessDate` next to `_seedDemoWageRoleRows` (the canonical populated demo path) — not `_onCreate`, exactly as Slice B/C wage/per-location seeds (accepted precedent). | PASS — `sqlite_database.dart` reseed path is the proven populated bootstrap (Slice B `_seedDemoWageRoleRows` is reseed-only and shipped); cold-boot `_onCreate` has no `_seedDemoWageRoleRows` so a wage override there would lack its baseline — reseed-only is consistent + lower risk. |
| 10 | Notification renders true-to-logic on the surface | `app_notifications` row uses `${rid}_$eventKey` id shape + `type/event_key/title/body/business_date/created_at/read_at` exactly like `AppNotificationService`; unknown `event_key` → `NotificationsScreen._fallbackCopyFor` returns raw title/body, `_accentFor`/`_iconFor` default cases. | PASS — re-read `notifications_screen.dart:418-494`: graceful default path confirmed; `read_at=null` → counts toward bell badge + renders unread, identical to a runtime emit. |
| 11 | Operator-web mirror correctness | `kDemoWageRoleRowScopeFixture` keyed by `kDemoTeamLocationsFixture` ids (`demo-loc-*`) + `operatorId='demo-operator'` (router passes `session.operatorId` + `locationScope.id`, `operator_web_router.dart:1214-1220`); demo gateway `list()` filters op+loc+active. | PASS — Riverside blend 17.50/22.35 ≠ Downtown 16.50/21.35; Harbour/North-Loop == business default; test asserts blends + unique ids + operator. `wage_authority_screen_test` + `data_accuracy_screen_test` green (47 passed) — no regression from the default-fixture change. |
| 12 | dart analyze | Clean on all touched files + new test. | PASS — `dart analyze` → "No issues found!". |
| 13 | Regression — prompt-named suites green, no seed regression | `per_daypart_v1_demo_seed_per_period_cycle_test` (per-period cycle) + `target_cycle_daypart_pool_test` + `per_daypart_v1_demo_seed_per_location_data_test` (#807) all green alongside new suite. | PASS — single run `+20 All tests passed` (7 new + 13 regression); + `app_notification_service`/`slice_a_hierarchy`/`wage_authority_screen`/`data_accuracy_screen` `+47 All tests passed`. CI dark — local only. |
| 14 | Tests green (new + nearest HP#11/notifications) | New 7-test suite covers Region/Location/District effective+inherited, Metric-Honest notification, determinism, HP #4, web fixture mirror. | PASS — all green; no `.skip`/`xfail`. |

## Local commands (CI dark — disclosed)

- `powershell scripts/install_git_hooks.ps1` → "Forge & Flow git hooks enabled" (pre-commit, pre-push).
- `flutter pub get` → Got dependencies!
- `dart analyze lib/infrastructure/persistence/sqlite/sqlite_database.dart lib/operator_web/services/operator_web_wage_authority_gateway.dart test/per_daypart_v1_demo_slice_f_hp11_overrides_notifications_test.dart` → **No issues found!** (`sqlite_database_seed.dart` is a `part` of `sqlite_database.dart` — analyzed via the library file.)
- `flutter test test/per_daypart_v1_demo_slice_f_hp11_overrides_notifications_test.dart test/per_daypart_v1_demo_seed_per_period_cycle_test.dart test/target_cycle_daypart_pool_test.dart test/per_daypart_v1_demo_seed_per_location_data_test.dart` → **+20 All tests passed!**
- `flutter test test/app_notification_service_test.dart test/per_daypart_v1_demo_slice_a_hierarchy_test.dart test/operator_web/screens/wage_authority_screen_test.dart test/operator_web/screens/data_accuracy_screen_test.dart` → **+47 All tests passed!**

## Residual / notes for orchestrator

- **Web inherited-source PILL is Gap 32 (wage) / Gap 29 (data-accuracy)** per spec §1.7 — the operator-web Wage authority screen's scope is hardcoded to `HierarchyScopeLevel.location` and there is no data-accuracy `HierarchyScopeNotice` yet, and there is **no in-memory demo data-accuracy gateway** at all (only `OperatorWebHttpDataAccuracyGateway`). Building those resolvers/gateways is feature work outside a demo-DATA slice and outside the prompt's file list (router untouched). Per HP #11 rule 11 this is documented as gated/incomplete: this slice shapes the **data** so the effective values are correct (mobile, true-to-logic per `restaurant_id`) and mirrors the wage scope into the existing wage demo gateway; the moment the wage/data-accuracy inherited-source notices land, the inheritance renders correctly with zero further seed work.
- Mobile expresses scope as per-`restaurant_id` rows + ABSENCE-means-inherit (spec §4 watch item, line 210) because the SQLite settings tables carry no `org_unit_id`/scope-level column. The §2c corp→region→district→location tree lives in the operator-web fixture (`demo_team_fixtures.dart`, unchanged). North Loop carries BOTH the Region (timing) and District (data-accuracy) overrides because it is the sole East-Region location that is also the sole Metro-District location — deliberate, keeps each override single-axis.
- Reseed-only call placement (lens 9) mirrors the accepted Slice B/C precedent; if a future cold-boot-without-reseed demo path is added, these calls should be added to `_onCreate` alongside a `_seedDemoWageRoleRows` call (same precondition gap exists for Slice B today).
