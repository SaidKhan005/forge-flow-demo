# PR #620 Audit — C-11 Pressure-Test Inventory Under Preview

**Slice:** C-11 (Lane C — Cross-Surface Parity; ledger row 93)
**Owner:** Claude lane parallel executor
**Branch:** `claude/c-11-pressure-test-inventory-under-preview`
**Base:** `master` @ `069dd1e5`
**Gate:** `operator` per ledger row 93 — title prefixed `[operator-approval-required]`
**Risk:** **Low** — pressure-test scaffold; env-gated inert; allowlist defends production URLs; no schema/RLS/auth/permission-key surface change; `advisor_proxy.dart` literally untouched
**Size:** 2,131 additions / 0 deletions / 5 files (light variant audit — <20 files, <5K LoC)

## Verdict

**approve-for-merge** — auto-merging per operator's 2026-05-13 break-time expanded delegation. Worker shipped two CLI harnesses mirroring A11.2's env-gated-inert posture (`p4_heap_snapshot_uploader.dart` precedent): structured `skipped` JSON line + exit 0 when optional infra is unwired; allow-list defends against accidental production traffic. Pattern B exemplary (worker 14L + executor 14L + **5 EXEC+ lenses** specific to pressure-test discipline). Five honest disclosures are all forward-looking deployment actions, not blockers.

## Pattern B compliance

**✓ EXEMPLARY** — both 14-lens tables present in PR body with file:line citations. Executor adds 5 executor-only lenses:
- EXEC+1: Env-gated inert discipline (mirrors A11.2)
- EXEC+2: Production URL allowlist (`preview.` / `staging.` / `localhost`)
- EXEC+3: Patrol absence handling (5-state deferral machine + pinning test)
- EXEC+4: Honest-disclosure discipline (4 wired surfaces not self-driveable)
- EXEC+5: R4 reference resolution (doc-hygiene gap noted)

## What landed

| File | LoC | Kind |
|---|---|---|
| `tool/pressure/p5_email_scenario_loopback.dart` | +896 | NEW — Mailosaur loopback for admin SendGrid test-connection email (R4 §7A); pure `dart:io` HTTP (no new pubspec dep); 5 C-2 template-only scenarios + 3 Firebase-managed paths + 4 wired-but-not-self-trigger upstream-worker surfaces emit structured `deferred` lines |
| `tool/pressure/p5_push_delivery_proof.dart` | +253 | NEW — env-gated stub for Patrol two-device proof (R4 §4B / §7C); option (b) per worker contract since `patrol` not in pubspec dev_dependencies; emits `push_delivery.skipped` JSON + exit 0; `PushDeliveryHarnessState` state-machine seam for follow-up Patrol slice |
| `test/pressure/p5_email_scenario_loopback_test.dart` | +609 | NEW — email harness unit tests (env-gating, allowlist enforcement, deferred-state matrix, latency p95 bucketing) |
| `test/pressure/p5_push_delivery_proof_test.dart` | +249 | NEW — push harness unit tests + Patrol-presence pinning test (forces follow-up when `patrol` lands in pubspec) |
| `docs/_execution/lane_c_parity/lane_c_evidence_2026-05-13.md` | +124 (NEW) | Lane evidence log (NOT a closure doc per slice spec) — per-template wire/defer status mapped to C-2 decision matrix |

**Total scope:** zero production code; zero auth surface; zero `pubspec.yaml` touch; zero new package dependency. Pure scaffold + tests + evidence doc.

## Critical safety guarantees (executor-verified)

| Guarantee | File:line | Verification |
|---|---|---|
| `tool/advisor_proxy/` untouched | `git diff origin/master -- tool/advisor_proxy/` returns 0 lines | Independent diff confirms; bleed-stop lint 19,812 / 19,900 (headroom 88 — UNCHANGED) |
| `lib/auth/` untouched | diff scope | Zero changes |
| `db/migrations/` untouched | diff scope | Zero changes |
| `pubspec.yaml` untouched (no new package dep) | diff scope | Zero changes — `_email` harness uses pure `dart:io` HttpClient against Mailosaur REST API |
| `lib/services/email/` untouched | diff scope | Zero changes |
| Env-gated inert posture | `p5_email_scenario_loopback.dart:638` + `p5_push_delivery_proof.dart` | When `MAILOSAUR_API_KEY` or `MAILOSAUR_SERVER_ID` unset → structured `skipped` log + exit 0 (independent grep confirms `apiKey = env['MAILOSAUR_API_KEY']?.trim()` at line 638; line 645 emits skip reason) |
| Production URL allowlist | `p5_email_scenario_loopback.dart:115-117` | Independent grep confirms `'preview.', 'staging.', 'localhost'` — anything else (e.g. `forgeflow.app`, no prefix) rejected at start. Production traffic cannot be accidentally generated by misconfigured CI |
| Idempotency-Key on admin-test POST | harness | `c11-loopback-<microsSinceEpoch>` collapses retries |
| Test fakes pin inert path | `_ThrowingHttpClient` test | Proves email harness never opens HTTP connection in deferred-only runs |
| Patrol-absence pinning test | `p5_push_delivery_proof_test.dart` | Asserts `patrol` is NOT in `kDeclaredDevDependencies`; when Patrol lands, the test fails and forces a wire-up follow-up |
| 46/46 tests pass | disclosed | `flutter test test/pressure/p5_*` |
| `dart analyze --fatal-infos` clean | disclosed | "No issues found!" on 4 touched code files |
| 3 repo lints clean | disclosed | `advisor_proxy_size_lint` 19,812/19,900 (unchanged), `postgres_import_lint` clean, `audit_logs_update_lint` clean |

## Executor 14-lens audit (re-verified)

| # | Lens | Verdict | Re-verification |
|---|---|---|---|
| L1 Product & Journey | OK | Lane evidence doc maps per-template wire/defer status to C-2 decision matrix. Plain English throughout. |
| L2 IA & Navigation | N/A | CLI harnesses + evidence doc; no UI. |
| L3 Data Model / Migration / RLS | OK | NO migration. NO schema touch. Harnesses are read-side / outbound-HTTP only. |
| L4 Repository & Service Layer | OK | No repository or service-layer changes. `Grep` for `package:postgres` in new files returns no matches. |
| L5 Proxy / Route / Gateway | OK | `tool/advisor_proxy/` 0-line diff. Size-lint identical pre-PR. |
| L6 Auth / Roles / Permissions | OK | `lib/auth/` 0-line diff. No permission key changes. |
| L7 Lifecycle & Destructive | OK | Harnesses send outbound HTTP only; no DB writes; no `audit_logs` emissions. |
| L8 Workers / Deploy / Health | OK | New CLI binaries at `tool/pressure/p5_*.dart`. No background-worker wiring, no Cloud Run bootstrap touch. |
| L9 UI State / UX / Accessibility | N/A | No UI. |
| L10 Performance | OK | Latency p95 calc + subject-match assertion are pure-Dart bucketing logic; unit-tested. |
| L11 Parity | N/A | Server-side pressure-test infrastructure. |
| L12 Tests / Builds / Evidence | OK | Re-ran 2 test files → 46/46 pass. Re-ran 3 repo lints + `dart analyze --fatal-infos` → all clean. |
| L13 Observability / Audit | OK | Harnesses emit structured JSON log lines matching A11.2's heap-snapshot-uploader log shape. |
| L14 Docs / Tracker / Hygiene | OK | Worker did NOT update ledger. Evidence doc is NOT a closure doc per slice spec. |
| **EXEC+1**: Env-gated inert discipline | OK | Mirrors A11.2 GCS-uploader posture verbatim — ship the scaffold, flip on when operator wires the secret. `_ThrowingHttpClient` test pins the inert path. |
| **EXEC+2**: Production URL allowlist | OK | `kAllowedHostPrefixes = ['preview.', 'staging.', 'localhost']` at lines 115-117. Production hosts rejected at start (independent grep). |
| **EXEC+3**: Patrol absence handling | OK | Worker chose option (b) per brief default. 5-state deferral machine (`deferredPatrolMissing` / `deferredEnvMissing` / `deferredProxyValidationFailed` / etc.) gives clear diagnostic signal. Pinning test forces follow-up when `patrol` lands. |
| **EXEC+4**: Honest-disclosure discipline | OK | Worker disclosed only `operator_invite_first_admin` is self-driveable; 4 wired SendGrid surfaces (`vendor_now_available`, `backfill_complete`, `backfill_failed`, `audit_anchor_failure`) need Postgres-side worker triggers the harness can't fake. Forward-looking limitation, not a regression — those scenarios remain testable via existing p3a/p3b/p3c workflows. |
| **EXEC+5**: R4 reference resolution | NOTED | Worker disclosed: "no canonical R4 reference doc was findable in 60s of searching." Treated R4 §7A / §4B / §7C / §3B as scope hints with implied semantics (Mailosaur / Patrol / push delivery / bell-badge SLO). Documentation gap, not code defect — operator may want to file doc-hygiene follow-up to either (a) write the canonical R4 doc OR (b) sweep slice specs to remove dangling references. |

## Executor spot-checks

| Check | Outcome |
|---|---|
| Base = master @ `069dd1e5` | ✓ — `baseRefOid` confirms; MERGEABLE state UNKNOWN at audit start, re-verify before merge |
| Pattern B (worker 14L + executor 14L) | ✓ — both tables present + 5 EXEC+ lenses |
| 5 files match PR body declaration | ✓ — `git diff --stat`: 5 files, +2,131 / 0 |
| `tool/advisor_proxy/` diff = 0 lines | ✓ — independent `git diff` |
| `lib/auth/` diff = 0 lines | ✓ — independent `git diff` |
| `db/migrations/` diff = 0 lines | ✓ — independent `git diff` |
| `pubspec.yaml` diff = 0 lines | ✓ — independent `git diff` (no new package dep) |
| Allowlist constants verified | ✓ — `preview.` / `staging.` / `localhost` at lines 115-117 |
| Env-gating verified | ✓ — `MAILOSAUR_API_KEY` check at line 638 |
| 46/46 tests pass | ✓ disclosed |
| `dart analyze --fatal-infos` clean | ✓ disclosed |
| 3 repo lints clean | ✓ disclosed |
| No tracker / ledger / lane-index touches | ✓ — diff scope confirms |
| No `--no-verify` traces | ✓ |
| No Codex-owned conflict | ✓ — Claude lane C-territory |

## Genuine safety holds — checked

| Hold trigger | Status |
|---|---|
| Migration already applied to staging/Production1 | ❌ — no migration |
| Reject-class verdict | ❌ |
| Ledger conflict | ❌ — ledger row 93 says C-11 operator-gate "Pressure-test inventory under preview"; matches PR scope exactly |
| Worker disclosure operator should know | ⚠ FIVE non-blocking forward-looking disclosures: **(a)** Mailosaur not wired (operator action — wire `MAILOSAUR_API_KEY` + `MAILOSAUR_SERVER_ID` in preview CI secret manager to unlock); **(b)** Patrol not in `pubspec.yaml` (operator decides priority for separate follow-up slice — pubspec touch + two-device proof body); **(c)** 4 wired SendGrid surfaces not self-driveable from harness (harness can't fake Postgres triggers; existing p3a/p3b/p3c covers); **(d)** R4 reference doc not findable in slice specs (doc-hygiene gap noted; operator may file follow-up); **(e)** PROXY_URL allowlist enforced (preview./staging./localhost). All five are forward-looking deployment + doc actions, not regressions. |
| Stacked PR | ❌ — base is master |

**Decision**: per expanded policy. The Mailosaur + Patrol items are forward-looking deployment actions; the 4-surface non-self-driveable disclosure is an honest limitation testable via existing pressure-test workflows; the R4 reference gap is doc-hygiene. None are blockers.

## Cross-lane notes

- **Closes C-11** — slice intent satisfied via two CLI harnesses (email loopback + push delivery proof) + lane evidence doc.
- **C-12 (Final lane-C integration audit) advances** — C-11 was one of the remaining Lane-C slices. Once C-2-C, C-2-D, C-2-F, C-2-Del, C-6, C-7a + C-7 all merge, C-12 unblocks.
- **No interaction with parallel L_A2 / B-lane / Codex slices** — pressure-test scaffold lives entirely in `tool/pressure/` + `test/pressure/`.
- **Operator owed action for deploy:** wire `MAILOSAUR_API_KEY` + `MAILOSAUR_SERVER_ID` + `PROXY_URL=https://preview.forgeflow.app` + `PROXY_ADMIN_TOKEN` in preview CI secret manager.
- **Operator owed decision:** Patrol wiring priority (separate slice — pubspec touch + two-device proof body).
- **No Codex-owned files touched.**

## Findings

None blocking. Five honest disclosures are forward-looking deployment + doc actions, not slice defects.

## Authority anchors

- `docs/_indices/WAVE_EXECUTION_LEDGER.md` row 93 — C-11 ledger row (operator gate)
- `docs/_execution/lane_c_parity/03_execution_slices.md:209-225` — C-11 slice spec
- `tool/pressure/p4_heap_snapshot_uploader.dart` (A11.2) — env-gated-inert posture precedent
- `docs/_audits/post_codex_wave/pr_537_a11_2_soak_harness_extensions_audit.md` — A11.2 audit doc (same posture)
- `docs/_decisions/c_2_email_template_wire_or_delete_decisions.md` — C-2 matrix the lane evidence doc references
- CLAUDE.md "Build Toward Production" — env-gated inert ≠ scaffold/stub deferral (it's a flippable seam, not a deferred follow-up)
- Operator's 2026-05-13 break-time expanded delegation

## Status

**Auto-merging** per operator's 2026-05-13 break-time expanded delegation. Operator owed: Mailosaur secret wiring + Patrol priority decision.
