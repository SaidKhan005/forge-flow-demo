# Known Failing Tests

Quarantine list for tests known to be red on `master` that are out of scope
for the current slice.

Read this before claiming a regression: a failure here is pre-existing and
does not block the current slice unless the slice explicitly names it.

Codex maintains this file. Add an entry when verification confirms a failure
is pre-existing on clean HEAD. Remove an entry once the failure is fixed.
Removed entries live in git history; do not keep a "resolved" section here.

## Open

| File | Notes | Discovered | Owning slice |
|------|-------|------------|--------------|
| `test/pressure/p3c_oauth_refresh_storm_runner_test.dart` | Two assertions in the "Closure registry coverage" group drift from production: (a) `oauth-flavored vendors NOT in the worker registry` expects `containsAll(['agendrix', 'opentable', 'adp', 'sevenrooms'])` but the registry has been pared back to `['sevenrooms', 'agendrix']` only; (b) `worker registry wires exactly the 11 expected OAuth vendors` expects `hasLength(10)` but the wired set is now `hasLength(12)`. Pre-existing on clean master at the 2026-05-12 A10.1 consolidation pass; reproduced before the consolidation. The other 5 group cases pass. | 2026-05-12 | follow-up — re-pin the assertions against the current `buildProductionRefreshClosures` + `kVendorsWithoutRefreshClosure` constants, or split into a generated reconciliation finding |
| `test/tool/advisor_proxy/admin_integrations_response_sanitization_test.dart` | Pre-existing `_StubHttpHeaders.forEach` shape mismatch in the dispatch wrapper sanitization test. Surfaced in PR #634 (B6 decompose) audit doc as a pre-existing failure verified on clean master, not introduced by B6. Surfaced again in wave-closeout `wave_audit_honest_disclosures.md` (U-1) and `wave_audit_test_coverage.md` (P3). | 2026-05-13 (wave closeout) | follow-up — update `_StubHttpHeaders` mock to match current `dart:io` `HttpHeaders.forEach` typedef OR widen the wrapper test |
| `test/infrastructure/persistence/postgres/repositories/weekly_plan_snapshot_repository_test.dart` | Compile error: references `PackagePostgresPool` without importing it (PR #459, pre-wave). Blocks 44 sibling tests in `test/infrastructure/persistence/postgres/repositories/` from running. Surfaced in wave-closeout `wave_audit_test_coverage.md` (P2). | 2026-05-13 (wave closeout) | follow-up — 1-line import add of `package_postgres_pool.dart` |
| `test/operator_web/widgets/vendor_relativity_label_test.dart` | "QuickBooks Time connected (poll-only) — copy mentions QBT and F&F controls cadence" — pre-existing on master. Disclosed by Wave 2 U-FU-summary-strip-cleanup (PR #710) and S-3 (PR #717). | 2026-05-14 (Wave 2 closeout) | follow-up — re-pin the expected copy against the current `vendor_relativity_label.dart` text |
| `test/infrastructure/persistence/postgres/repositories/weekly_plan_snapshot_repository_test.dart` | 4 `PackagePostgresPool` analyzer errors — pre-existing on master, distinct from the earlier "1-line import add" entry above. Disclosed by Wave 2 W-3 (PR #697). | 2026-05-14 (Wave 2 closeout) | follow-up — pairs with the earlier `weekly_plan_snapshot_repository_test.dart` entry; full fix likely needs both the import add + corrected pool wiring |
| operator-web router test — `management picker drives location-scoped vendor route` | Flaky `pumpAndSettle` timeout on the operator-web vendor-connections route: the test pumps the management picker → vendor-connections route and `tester.pumpAndSettle()` exceeds its deadline (a never-settling animation/timer on that route in the test harness, not a behavior regression). Reproduces on pristine master with no slice changes applied; unrelated to the cross-surface-parity / G-series slices. Re-flagged repeatedly by agents as a suspected regression — quarantined here so it stops being re-reported. | 2026-05-16 | follow-up — pump with a bounded `pump(Duration)` loop instead of unbounded `pumpAndSettle`, or stub the never-settling timer on the vendor-connections route under test |
| `test/widget/shift_dashboard_daypart_parity_test.dart` | One pre-existing whole-day Shift assertion is red on clean master: "Per-Daypart V1 Slice 4 — Shift daypart card full parity · whole-day LABOR card render is unchanged — Theoretical sub-line + delta pill still present (Promise 3 / Layer 9, byte-untouched)" — `find.textContaining(' pts')` returns 0 after scrolling to the whole-day LABOR card (the `_LaborVarianceSection` ±pts delta pill is not found by the scroll-to-visible probe). Whole-day-path assertion, unrelated to the Whole Day operating-envelope header work. Verified by re-running the file against a pristine `git worktree` at HEAD `299a9c52` (zero changes) — same `+7 -1` shape — during PR #897. Same family/root cause as the `shift_visual_widget_test.dart` row below (whole-day LABOR/Primary-Driver scroll-target drift). Quarantined so agents stop re-reporting it as a PR #897 regression. | 2026-05-16 (PR #897 baseline check) | follow-up — re-pin against the current whole-day `_LaborVarianceSection` ±pts delta-pill copy / scroll target in `shift_dashboard.dart` (pairs with the `shift_visual_widget_test.dart` re-pin below) |
| `test/shift_visual_widget_test.dart` | Two pre-existing whole-day Shift assertions are red on clean master: (a) "ShiftDashboard smoke · renders provider-backed dashboard scaffolding" (`:191`) — `find.text('DRIVER')` returns 0 after `_scrollUntilMetricVisible` reaches the hero metric; (b) "F — reservation book signal · COVERS card renders 'In the books 72' when reservation exists" (`:576`) — `find.text('In the books 72')` returns 0. Both are whole-day-path assertions (Primary-Driver chip text; `targetSupportFormatted` reservation line), unrelated to the per-daypart MetricPill work. Verified by re-running the file against a pristine `git worktree` at HEAD `857bccb7` (zero changes) — same `+? -2` shape — during PR #868. Quarantined so agents stop re-reporting it as a PR #868 regression. | 2026-05-16 (PR #868 baseline check) | follow-up — re-pin (a) against the current Primary-Driver chip copy/scroll target and (b) against the current COVERS `targetSupportFormatted` ("In the books N") render path in `shift_dashboard.dart` |
