# Wave Execution Ledger

Single-source-of-truth tracker for every slice across the post-Codex wave.

Updated: 2026-05-12 (initial — all slices `assigned`).
Owner: orchestrator (this Claude session) is the only writer; executors read.

## How this works

- **Executors** (Codex session + Claude lane session) read this ledger before picking their next slice. Pick the first slice with `state = assigned` on your lane. Lock it by opening the PR (you don't edit this file).
- **Orchestrator** (the audit/merge Claude session) updates `state` and `pr` columns post-merge.
- **State machine**: `assigned` → `in-progress` (PR open) → `audit-pending` (PR approved by orchestrator audit but waiting on operator gate) → `merged` (on master). Reject path: `audit-pending` → `back-to-author` (executor reopens or follow-up PR).
- **Gate column**: `auto` (orchestrator merges after clean audit), `operator` (operator approves before merge; orchestrator pings).
- **Owner column**: `Claude` (Claude lane session) or `Codex` (Codex session) or `orchestrator` (this session — A0 verification probe + C-12 closeout).
- **Dependency column**: must be `merged` before the dependent slice opens.

## Concurrency rule

Two executors must not work the same slice. If your lane's first `assigned` slice is also another executor's lane (cross-lane dependency), wait until that one merges. The dependency column makes this explicit. If a dependency hasn't merged but the dependent slice is non-blocking, you can start in parallel — note that explicitly in your PR description.

## Operator-approval gates (recap)

Triggers the `operator` gate: auth-critical, RLS-touching, schema-touching (migration), proxy-touching, demo-mode reader carve-out, KMS, billing, vendor-live carve-out.

## Slice ledger

### Lane A — Code Health

| Slice | Plan anchor | Owner | Size | Risk | Gate | Dependency | State | PR | Notes |
|---|---|---|---|---|---|---|---|---|---|
| A0 | `lane_a_code_health/03_execution_slices.md` Slice A0 | orchestrator | Small | Low | auto | — | merged | — | Verification PASS 2026-05-12; doc: `docs/_audits/code_health/a0_b1_b2_post_merge_verification.md` |
| A2.1 | Slice A2.1 | Codex | Small | Low | auto | A0 merged | merged | #519 | Dead-code sweep — 2 placeholder widgets deleted (179 LoC removed); inventory updated |
| A2.2 | Slice A2.2 | Codex | Medium | Medium | operator | A2.1 merged | merged | #540 | Email pipeline wire-or-delete — deleted 2 dormant SendGrid templates (`password_reset_request.md`, `operator_admin_invite.md`) + their registry entries; preserved `operator_invite_first_admin.md` as admin SendGrid test fixture. Live flows use Firebase action-link email (verified). 21/21 tests pass. Operator approved 2026-05-13. |
| A3.1 | Slice A3.1 | Claude | Medium | Medium | operator | A0 merged | merged | #533 | Monolith seam-map (12 clusters with file:line ranges) + bleed-stop CI lint (`advisor_proxy_size_lint`) — read-only inspection of `advisor_proxy.dart`. Current 18,871 / ceiling 19,071 / 200-line headroom. Operator approved 2026-05-13. Unblocks A3.2 (bare-catch tail chunk 1 of 3). |
| A3.2 | Slice A3.2 | Claude | Medium | Medium | operator | A3.1 merged | merged | #563 | Bare-catch tail chunk 1 of 3 — 12 sites converted (clusters 2-3 + early 4 per A3.1 seam map): JWT verification, JWKS, signature, ProxyUsageGuard. 1 bare catch intentionally retained at line 2416 (`Platform.environment` swallow; `UnsupportedError` is an `Error` not `Exception`, narrowing would break fallback). `on Object` deliberately never used so genuine `Error`s propagate. Operator approved 2026-05-13 as Option A (accept cluster-aligned chunking 12+16+16 across A3.2/A3.3/A3.4 + pure-refactor reasoning for the test gap — typed catches preserve all observable behavior except `Error` propagation, which is the intended improvement). 2 files / +68/-16. `dart analyze` clean; bleed-stop lint 18,899/19,071 (headroom 172). Audit doc: `docs/_audits/post_codex_wave/pr_563_a3_2_bare_catch_typing_chunk_1_audit.md`. **Unblocks B11.2.b** — all 3 deps now merged. |
| A3.3 | Slice A3.2 (chunk 2 of 3) | Claude | Medium | Medium | operator | A3.2 merged | merged | #572 | Bare-catch tail chunk 2 of 3 — 5 sites typed in clusters 4-8 (came in lower than ~16 estimate because clusters 4-8 are sparser than seam map suggested; A3.4 revised ~16 → ~22 from fresh grep). All 5 sites narrowed to `on Exception catch (_)` with 5-line rationale comments naming concrete throw classes (postgres `PgException`, `TimeoutException`, `IOException`, etc.). 2 retentions preserved correctly: line 2426 (`Platform.environment` swallow — `UnsupportedError` is `Error` not `Exception`); line 7024 (`SessionRecordIncompleteGauge.observe()` — out-of-scope per slice brief, owned by A11.1.b). 2 files / +76/-11. Bleed-stop lint 18,904 → 18,934 (+30; headroom 137). Operator approved 2026-05-13 as Option A continuation. Pattern B exemplary (worker 14L + executor 14L). Audit doc: `docs/_audits/post_codex_wave/pr_572_a3_3_bare_catch_typing_chunk_2_audit.md`. |
| A3.4 | Slice A3.2 (chunk 3 of 3) | Claude | Medium | Medium | operator | A3.3 merged | merged | #581 | **Completes the operator-approved 45-site bare-catch sweep**: A3.2 (12) + A3.3 (5) + A3.4 (26) = **43 typed** + 2 retained (lines 2426 + 7107) + 1 B10.1 carry-forward at line 14109 = 45 sites accounted for. 25 sites narrowed to `on Exception catch (_)`; 1 site narrowed to `on FormatException` (the `Base64Decoder` site at 16707 — narrowest possible). `on Object` deliberately NEVER used per addendum C4. Each conversion carries 1-3 line rationale comments naming concrete throw classes. 2 files / +217/-31. 248 existing tests pass. Pattern B exemplary (worker 14L + executor 14L). Operator approved 2026-05-13 via break-time delegation. Honest disclosures (all non-blocking): (a) self-disclosed `git stash` use during baseline cross-check, cleanly recovered via `git fsck --lost-found`, final diff audited clean; (b) pre-existing master size-lint regression from B10.1's +400 lines without ceiling raise (master 19,458 / 19,071; A3.4's +78 was inside ratchet at base SHA); (c) 5 pre-existing test failures in `admin_cors_bootstrap_test.dart` reproducible on master pre-A3.4 (probably B10.1 fallout); (d) B10.1 carry-forward bare-catch at line 14109. Triple-safeguard verified (35 typed sites on master post-merge). Audit doc: `docs/_audits/post_codex_wave/pr_581_a3_4_bare_catch_typing_chunk_3_audit.md`. **A3.x cluster CLOSED.** Follow-ups owed: bleed-stop ceiling raise + B10.1 carry-forward fix + admin_cors_bootstrap_test investigation. |
| A4.1 | Slice A4.1 | Claude | Small | Low | auto | A0 merged | merged | #517 | Performance audit pass — 3 ranked recs delivered for A4.2 (Postgres pool 4→20, shift_dashboard ticker coalescing, healthProducerConcurrency wiring) |
| A4.2 | Slice A4.2 | Claude | Medium | Medium | operator | A4.1 merged | merged | #552 | Performance fixes — all 3 A4.1 ranked recs: R1 (Postgres pool default 4→20 fallback), R2 (4 shift_dashboard 30s timers → 1 shared `_ShiftDashboardTicker` ValueNotifier), R3 (`healthProducerConcurrency` env-resolved). 11 files / +608/-288. Behavior-preserving. Operator approved 2026-05-13. Unblocks B11.2.b wiring slice. |
| A5+A8 | Slice A5+A8 | Codex | Medium | Medium | operator | A0 merged | merged | #527 | Schema versioning post_deploy convention + `audit_logs` UPDATE allowlist lint + `--require-expand-contract` scanner mode; grandfathered to B11.1's migration; operator approved 2026-05-13 |
| A6.1 | Slice A6.1 | Codex | Small | Low | auto | — | merged | #498 | Proxy health UI honesty pass |
| A7.1 | Slice A7.1 | Claude | Small | Low | auto | A4.1 merged | merged | #526 | Frameworks cross-reference sweep — picked canonical `docs/frameworks/<NAME>.md` path; fixed 7 broken refs across 3 active docs; `docs/frameworks/README.md` rewritten with correct 5-framework index |
| A9.1 | Slice A9.1 | Codex | Medium | Medium | operator | A0 merged | merged | #530 | `lib/data` rehome — 3 frozen-legacy files moved (app_defaults + cross_axis_pair_catalog → `lib/domain/constants/`; mock_integration_replay_seed → `lib/dev/`). 72 files / +90/-90 (mostly import-path updates). 3 active contract docs updated. `lib/data/` empty on master post-merge (delete-only doctrine fully honored). Operator approved 2026-05-13. |
| A10.1 | Slice A10.1 | Claude | Small | Low | auto | A0 merged | merged | #523 | Test consolidation — 9 git mv `test/load/pressure/` → `test/pressure/`; closes 1 stale `KNOWN_FAILING_TESTS` entry + adds 1 for pre-existing closure-registry drift |
| A11.1 | Slice A11.1 | Claude | Small | Medium | operator | A0 merged | merged | #522 | Production `proxy.session_record.incomplete{route, missing_field}` gauge — observability-only at `POST /v1/auth/session/login`; operator approved 2026-05-13; unblocks A11.2 |
| A11.2 | Slice A11.2 | Claude | Medium | Medium | operator | A11.1 merged | merged | #537 | Soak harness durable extensions — fd watcher (Linux), heap-snapshot uploader (storage-agnostic, GCS impl env-gated inert), p3c CLI flags. Operator approved 2026-05-13. **Follow-up locked in `docs/POST_HARDENING_FOLLOWUPS.md` P1 section**: swap GCS → Azure Blob before any soak run uses uploads. GCS env vars MUST stay unset until that swap lands. |
| A11.1.b | (new — 2026-05-13 deep audit P1) | Claude | Small | Low | auto | A11.1 merged | merged | #571 | **Session-record gauge consumer wiring.** Wave deep audit finding #2 fix: `SessionRecordIncompleteGauge` snapshot wired into `/health` envelope via new `sessionRecordIncompleteCountProducer` in the `infra` family. Bootstrap hoists a single shared gauge instance in `proxy_bootstrap.dart` (fixes a latent disjoint-counter bug where the snapshot side observed a different instance from the increment side). Defensive projection — null accessor → `not_wired`; throwing accessor → `producer_error`; empty steady state → `value: 0` (distinguishable from not-wired). PII-safety asserted (no `user_id`, `operator_id`, `location_id`, `session_id`, `email`, `ip` in metric labels). Mirrors `inMemoryBreakerStates` side-channel pattern at 6 sites. 7 files / +463/-4. Bleed-stop lint 18,904 → 18,957 (+53; headroom 114). Operator approved 2026-05-13 (auto-gate carve-out for observability-only proxy slices confirmed). Pattern B exemplary (worker 14L + executor 14L; worker undercounted the mirror sites — honest). **First Claude lane PR post-loop-mode restart — Pattern B drift trend reversed from 4/4 pre-restart → 0/2 post-restart.** Audit doc: `docs/_audits/post_codex_wave/pr_571_a11_1_b_session_record_gauge_consumer_audit.md`. |
| L_A1 | (new — 2026-05-13 B8 prereq, operator path A) | Claude | Medium | Medium | operator | — | merged | #608 | **Inheritance Tree primitive.** Foundation for any hierarchy-aware feature (B8 audit log filter, future region rollups, hierarchy-aware permissions, hierarchy-aware audit aggregates). Adds the `ltree`-based org-tree model (business → org-unit → location chain) with `OperatorScopedRepository` semantics. Operator-leading B-tree indexes on the parent/ancestor columns; RLS via `app_current_operator()` wrapper (no bare `current_setting()`); migration is additive expand. Sets up the `descendant_path` denormalization that L_A2 will cache. Promoted from "spec assumption" per operator 2026-05-13 (B8 Path A pick). |
| L_A2 | (new — 2026-05-13 B8 prereq, operator path A) | Claude | Medium | Medium | operator | L_A1 merged | merged | #616 | **Descendant-set cache for the Inheritance Tree.** Precomputed `(operator_id, scope_id) → descendant_location_ids[]` lookup table populated by trigger or background sync off `L_A1` writes. Lets hierarchy roll-up queries answer "everything under this scope" in one indexed lookup rather than walking the tree per request. Required for B8's hierarchy filter to scale past trivial org sizes. Wire RLS the same way (`app_current_operator()` wrapper). Migration is additive expand. Promoted from "spec assumption" per operator 2026-05-13 (B8 Path A pick). |

### Lane B — Features

| Slice | Plan anchor | Owner | Size | Risk | Gate | Dependency | State | PR | Notes |
|---|---|---|---|---|---|---|---|---|---|
| B1.a | `lane_b_features/03_execution_slices.md` B1.a | Claude | Small | Low | auto | — | merged | #501 | Inheritance notice propagation; copy follow-up applied by orchestrator (Option 1 per audit) |
| B1.b | B1.b | Claude | Small | Medium | operator | — | merged | #500 | Admin audit actor fix — operator approved 2026-05-12 |
| B1.c | (new — 2026-05-13 deep audit P1) | Claude | Small | Low | operator | B1.b merged | merged | #561 | Admin gateway actorKind peer-bug sweep — 4 sites flipped `actorKind: 'user' → 'forge_admin'` in `proxy_bootstrap.dart` for pricing/corpus/graph/integrations admin gateways. Mirrors B1.b idiom (PR #500). 4 audit-chain integrity tests added in `test/advisor_proxy_bootstrap_test.dart` (one per gateway, asserting `actorKind = 'forge_admin'` on the emitted audit row). Deep-audit doc finding #1 marked RESOLVED. Operator approved 2026-05-13. Salvaged from Claude lane relaunch — clean salvage path inspected. 3 files / +353/-9. Audit doc: `docs/_audits/post_codex_wave/pr_561_b1_c_actorkind_peer_bug_sweep_audit.md`. |
| B2.1 | B2.1 | Claude | Medium | Medium | operator | — | merged | #584 | F&F-wide versioned Default Role catalog: schema + repository + admin routes + admin gateway + production wiring. New `default_role_catalog_versions` table (F&F-global, no RLS — admin-pool BYPASSRLS posture; rationale documented in migration header). New `DefaultRoleCatalogVersionsRepository` (runAsSystem). New admin routes at `/v1/admin/auth/role-catalogs` + `/publish` with super_admin-only write gate + `Idempotency-Key` + actor resolution. `ProductionDefaultRoleCatalogAuditSink` writes via `AuthEventsAuditRepository.insertSystemEvent` → hash-chained fan-out to `audit_logs`; `actor_kind='forge_admin'` + admin_reason per 2026-05-13 contract. Bootstrap test asserts non-null + production-sink + not-noop (3 pins). Backwards-compatible resolver in `roles_repository.dart` for tenant-scoped catalog lookup. 18 files / +3437/-11 (265 migration + 458 repository + 167 resolver + 324 admin gateway + 515 router/sink + 85 dispatcher + 55 bootstrap + 8 main.dart + ~1544 tests). 71 disclosed local test passes. Pattern B exemplary (worker 14L + executor 14L; executor adds production-wiring-completeness lens + cross-operator-admin-posture lens). **Build-Toward-Production cycle**: worker 1 deferred production sink → executor sent back citing CLAUDE.md doctrine → worker 2 implemented → executor inlined 1-line main.dart threading per orchestrator-fix-inline default. Bleed-stop overshoot 19,628 / 19,600 (+28) handled in same orchestrator bundle (ceiling raise 19,600 → 19,700; Option A). Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_584_b2_1_default_role_catalog_audit.md`. **Migration pending apply to staging + Production1**. Unblocks **B2.2** (admin editor UI). |
| B2.2 | B2.2 | Claude | Medium | Medium | operator | B2.1 merged | merged | #590 | Default Role catalog admin editor + operator-web Default badge. UI-only on top of B2.1 backend (PR #584). Two honest gaps disclosed → future B2.3 (`GET /v1/admin/auth/role-catalogs/blast-radius`) + B2.4 (project `catalog_published_at` onto `is_seeded = true` role rows). |
| B3 | B3 | Codex | Medium | Medium | operator | — | merged | #502 | Role-key hybrid identifier sweep — operator approved 2026-05-12; unblocks B7.a |
| B4 | B4 | Codex | Small | Low | auto | B3 merged | merged | #513 | Two-product taxonomy in role editor + product-tagged audit payload |
| B5 | B5 | Codex | Medium | Medium | operator | — | merged | #557 | Admin access-control sweep — business-timing write role narrowed from {super_admin, ff_support} → {super_admin only} (reads unchanged); dispatcher route-ordering fix at `advisor_proxy.dart:14931` (load-bearing — without it the operator/location matcher swallows business-timing routes before the role gate applies); `admin_reason` added to 7 operator/location HTTP mutations (pairs with earlier server-side migrations). **Permission-key completeness half deferred** to B5.b. Operator approved 2026-05-13 03:22Z as Option A. Audit doc: `docs/_audits/post_codex_wave/pr_557_b5_admin_access_control_audit.md`. Unblocks B5.b. |
| B5.b | (new — operator decision 2026-05-13) | Codex | Medium | Low | operator | B5 merged | merged | #573 | **Catalog tri-mirror amendment for account + timing settings keys.** Per slice-owned follow-up doc `docs/_execution/lane_b_features/05.5_catalog_followup.md`. Tri-mirror landed: (a) `account.configure` + `business_timing.configure` added to `docs/contracts/auth_permission_key_catalog.md`; (b) same as constants `PermissionKeys.accountConfigure` + `PermissionKeys.businessTimingConfigure` in frozen `lib/auth/permission_keys.dart` (with allKeys set entries); (c) additive migration `db/migrations/202605131500_b5_b_catalog_tri_mirror.sql` seeds catalog rows (frozen=true, requires_mfa=false) + grants to super_admin + operator_owner + operator_admin only (operator_manager/supervisor/staff/ff_support correctly excluded); (d) 3 operator-web screen literals → constants. Migration is additive + idempotent (`ON CONFLICT DO NOTHING` on both inserts) + transactional. Permission-key lint surface expanded to catch future drift. New test `test/auth/permission_catalog_b5b_test.dart` (139 LoC) asserts the tri-mirror holds. 16 files / +336/-81. 54 disclosed local test passes. Pattern B full (worker 14L + executor 14L). Operator approved 2026-05-13 (delegation — "approve codex once audited and you are satisfied"). Triple-safeguard verified (constants live in `permission_keys.dart`; migration file present at expected timestamp). Audit doc: `docs/_audits/post_codex_wave/pr_573_b5_b_catalog_tri_mirror_audit.md`. **Migration pending apply** — `runbooks/phase_9_production1_migration_apply_runbook.md` updated. |
| B6 | B6 | Codex | Medium | Medium | operator | B10.1 merged | assigned | — | Benchmark override (hierarchy-inherited) |
| B7.a | B7.a | Codex | Small | Medium | operator | — | merged | #507 | Invite hierarchy-scope fix — operator approved 2026-05-12; Option A consumer-label follow-up applied by orchestrator |
| B8 | B8 | Claude | Medium | Medium | operator | L_A2 merged | assigned | — | Audit log hierarchy filter (ltree join). Depends on `L_A1` (Inheritance Tree primitive) + `L_A2` (descendant-set cache) per operator 2026-05-13 Path A pick. B8 itself becomes a thin consumer once the foundations are in place. |
| B9.1 | B9.1 | Codex | Small | Low | auto | — | merged | #510 | `/sign-in-security` 301 redirect on both dockerfiles |
| B9.2 | B9.2 | Codex | Medium | Medium | operator | B9.1 merged | merged | #547 | My Account consolidation + Active Sessions — 4-card IA (Profile/Security/MFA/Active Sessions); existing routes only; client-side MFA freshness gate (server-side step-up deferred to B11.2 per ledger row 66). 34/34 tests pass; `dart analyze` clean. Operator approved 2026-05-13 conditional on B11.2 server-side step-up being planned (confirmed in ledger). Unblocks B9.3 + C-7. |
| B9.3 | B9.3 | Codex | Small | Low | auto | B9.2 merged | merged | #568 | Adaptive 2FA card — 4-state machine (`MfaCardStage.{notEnrolled,enrolled,removalRequested,removable}`) reading server-backed factor/removal state from `/v1/auth/mfa/factors/list`. Plus client-side clock-skew handling in `firebase_operator_web_auth_source.dart` (`_isFreshForAccountMfa` with window + skew tolerance; complements B11.2.b's planned server-side fix at `web_account_gateway.dart`). 7 files / +1413/-51 (scope expanded vs ledger S/L estimate but coherent). 54 disclosed local test passes. Pattern B both tables present. Auto-merged 2026-05-13 04:00Z. Triple-safeguard verified (`MfaCardStage` confirmed live on master). Audit doc: `docs/_audits/post_codex_wave/pr_568_b9_3_adaptive_2fa_card_audit.md`. |
| B10.1 | B10.1 | Codex | Medium | Medium | operator | — | merged | #576 | `vendor_applicability` temporal table + RLS via `app_current_operator()` wrapper + operator-leading split unique indexes (F2 fix) + admin routes (super_admin only) with required `admin_reason` (F1 fix) + `Idempotency-Key` + operator read routes + admin/operator gateway clients + schema-validated JSONB metadata. 20 files / +3680/-149 (140 LoC migration + 441 LoC repository + 332 LoC admin gateway + 197 LoC operator-web gateway + 334 LoC metadata schemas + 477 LoC proxy routes + ~1463 LoC tests). 40 disclosed local test passes. Two send-back fixes (F1 required reason + F2 split indexes) applied cleanly. Pattern B full (worker 14L + executor 14L). Operator approved 2026-05-13 via delegation ("approve once audited and you are satisfied"). Triple-safeguard verified (migration file at expected path; repository class live on master). Audit doc: `docs/_audits/post_codex_wave/pr_576_b10_1_vendor_applicability_audit.md`. **Migration pending apply** to staging + Production1 per `runbooks/phase_9_production1_migration_apply_runbook.md`. Unblocks **B10.2** (vendor applicability UI). |
| B10.2 | B10.2 | Codex | Medium | Medium | operator | B10.1 merged | merged | #594 | Vendor applicability admin editor + operator-web wage authority binding. UI + operator-web binding on top of B10.1 backend (PR #576). Per-`setting_kind` tabs (wage/covers/polling). Operator-web Data Accuracy wage picker live-bound. `super_admin` write gate + required `admin_reason` + required `Idempotency-Key` per B10.1 contract. Temporal end/replace only (no delete). 11 files / +1876/-38. 46 disclosed tests pass. |
| B2.3 | (new — 2026-05-13 B2.2 follow-up) | Claude | Small | Low | auto | B2.2 merged | merged | #603 | Blast-radius endpoint for Default Role catalog publish. `GET /v1/admin/auth/role-catalogs/blast-radius?version_id=<uuid>` returns `{operator_count, location_count, user_count, version_id}`. B2.2 publish dialog now renders specced "Affecting N businesses, M locations, P users currently following version X" numerics; plain-English fallback preserved for error/zero/genesis. `runAsSystem` admin-pool BYPASSRLS for F&F-global cross-operator aggregate; single CTE pattern immune to concurrent operator-flip drift. Role gate `{super_admin, ff_support}` (B2.1 GET posture). 9 files / +1597/-42. 31 new tests + 42 regression-checked clean. Pattern B exemplary. Closes B2.2 Gap 1. |
| B2.4 | (new — 2026-05-13 B2.2 follow-up) | Claude | Small | Low | auto | B2.2 merged | merged | #609 | **Per-role `catalog_published_at` projection on operator-web roles screen.** Extend `WebTeamRolesGateway` / `TeamRoleCatalogEntry` to carry `default_role_catalog_version_id` + `catalog_published_at` per `is_seeded = true` role row. Update operator-web roles screen annotation from "Managed by Forge & Flow" to "Updated by F&F on <date>". Closes the second honest gap disclosed by B2.2 worker. Per operator's 2026-05-13 direction: "no business live yet so mutations are fine" — ship now, before first live operator. Likely a JOIN in the existing query; no migration expected. Auto-gate (operator-web read-only surface). |
| B11.1 | B11.1 | Claude | Medium | High | operator | — | merged | #512 | `handoff_codes` table + endpoints — operator approved 2026-05-12; Production1 apply deferred to next scheduled event; unblocks B11.2 + C-5 |
| B11.2 | B11.2 | Claude | Medium | High | operator | B11.1 merged | merged | #550 | RFC 9470 step-up challenge **scaffold** — schema (`auth_step_up_challenges` table + wrapper-only RLS + operator-leading indexes) + pure-function policy + dispatch router + RFC 9470 header builder + 27 tests pass. Operator approved 2026-05-13 as Option A: scaffold-only now + B11.2.b assigned for wiring. **Auth posture on master is unchanged** until B11.2.b lands — sensitive routes still accept any bearer token without freshness gate. Audit doc: `docs/_audits/post_codex_wave/pr_550_b11_2_step_up_challenge_audit.md` |
| B11.2.b | (new — operator decision 2026-05-13) | Claude | Medium | High | operator | B11.2 merged + A3.2 merged + A4.2 merged | merged | #586 | **Step-up challenge wiring + production binding + client adapters + B9.2 clock-skew P1 fix — auth-critical, HIGH risk.** All four scopes shipped together per Build-Toward-Production: (1) single choke-point at `routeRequest` head gates **20 sensitive routes** (vs ledger's 14 estimate; honest count disclosure) via registry-driven `kStepUpSensitiveRoutes` filter — implementation lives in new sibling files `tool/advisor_proxy/auth_step_up_gate.dart` (132 LoC) + `auth_step_up_routes.dart` (197 LoC including `ProductionStepUpAuditSink`); (2) new `StepUpChallengesRepository extends OperatorScopedRepository<T>` mirrors B11.1's `HandoffCodesRepository` verbatim — emit/consume via `withTenant`, `lookupForReplayCheck` via `withSystem` ONLY (oracle-safe — collapses cross-operator / wrong-route / wrong-user into single 401 to prevent probe-by-error-code leaks); (3) `lib/auth/fresh_mfa_resolver.dart` + `mfa_freshness_redirect_listener.dart` adapted (permission_keys.dart **UNTOUCHED**) + new `lib/operator_web/auth/step_up_challenge_handler.dart` parses `WWW-Authenticate: Bearer error="insufficient_user_authentication"` and replays with `Step-Up-Challenge-Id` HEADER (NEVER URL — addendum A1 explicitly enforced via dedicated `URL-param-ignored` test); (4) `web_account_gateway.dart:_requireFreshMfaToken` uses `freshMfaClockSkewWindow = Duration(seconds: 60)` (RFC 7519 §4.1.4 leeway) — boundary tests pin +0/+30/+59/+60-inclusive/+61-reject. 16 files / +2854/-12 (60 advisor_proxy.dart + 132 gate + 197 routes/sink + 59 bootstrap + 7 main.dart + 325 repository + 73 fresh_mfa_resolver + 15 freshness_listener + 320 step_up_challenge_handler + 55 proxy_client + 106 web_account_gateway + ~1517 tests). 58 new + 67 pre-existing sibling tests pass. Pattern B exemplary (worker 14L + executor 14L; executor adds choke-point-pattern-correctness + oracle-safe-replay-classifier lenses). **Build-Toward-Production discipline**: worker shipped main.dart threading INLINE (no B2.1 anti-pattern repeat). Audit-row shapes: `auth.step_up_challenge.required` + `auth.step_up_challenge.consumed` via INSERT to `audit_logs` (no UPDATE); challenge_id NEVER in raw form — only SHA-256 hex. Per-tenant tx wrapper defends against forged audit rows. Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_586_b11_2_b_step_up_wiring_audit.md`. **Auth posture changes**: sensitive routes now require fresh `auth_time`; one-shot step-up challenge if stale. **Unblocks C-7** (mobile parity for step-up). |

### Lane C — Cross-Surface Parity

| Slice | Plan anchor | Owner | Size | Risk | Gate | Dependency | State | PR | Notes |
|---|---|---|---|---|---|---|---|---|---|
| C-1a | (new — 2026-05-13 C-1 prereq, operator path A) | Claude | Small | Low | operator | — | merged | #599 | Prep migration for SendGrid Event Webhook idempotency — `email_event.provider_event_id text` NULLABLE + partial `UNIQUE INDEX email_event_provider_event_id_unique WHERE provider_event_id IS NOT NULL`. Pure additive expand. 7 files / +389/-11. 14 test cases pass. Pattern B exemplary. Migration queued for staging + Production1 apply at slot 43. **Worker disclosed possible C-1b need**: C-1's second gap (ECDSA pubkey column on `email_credentials`) NOT addressed in this slice. |
| C-1 | `lane_c_parity/03_execution_slices.md` C-1 | Claude | Small | Low | auto | C-1a merged | merged | #611 | SendGrid Event Webhook receiver. Depends on `C-1a` (UNIQUE INDEX prep migration) per operator 2026-05-13 Path A pick. C-1 can now rely on Postgres-enforced dedupe rather than soft-dedupe partial index. |
| C-2 | C-2 | Claude | Medium | Medium | operator | C-1 merged | merged (matrix; operator picks recorded 2026-05-13) | #617 | Wire-or-delete 6 template-only emails. **Shipped as a stop-and-disclose decision matrix** per the worker contract's Block 3 clause: drafts A + B already-resolved (A2.2 closure + addendum B4 deferral); drafts C/D/E/F/G stop-and-disclosed with architectural-decision rationale in `docs/_decisions/c_2_email_template_wire_or_delete_decisions.md`. **Operator picks 2026-05-13**: C/D/F WIRE; E/G DELETE. Follow-up slices C-2-C, C-2-D, C-2-F, C-2-Del queued below. |
| C-2-C | (new — 2026-05-13 C-2 wire pick) | Claude | Small | Medium | operator | C-2 merged | assigned | — | **Wire `mfa_factor_changed_notice` email.** Add `notif.mfa.factor_changed` catalog entry + hook helper + trigger-site call in `lib/services/mfa/mfa_removal_worker.dart`. Sub-path TBD at spawn: (a) wire from removal only [default — lower scope] or (b) wire from both + add enrollment hook (`mfa_enrollment_service.dart`). ~250-500 LoC. Auth-adjacent (Medium risk). |
| C-2-D | (new — 2026-05-13 C-2 wire pick) | Claude | Medium | Medium | operator | C-2 merged | assigned | — | **Wire `vendor_sync_error_alert` email.** Requires the first-failure-of-outage detector (per-row email would spam on transients). Likely a new `vendor_sync_outage_state` table or a CTE on `connector_sync_log` + hook + trigger from the polling tier. ~400-700 LoC. New state surface (Medium risk). |
| C-2-F | (new — 2026-05-13 C-2 wire pick) | Claude | Small | Medium | operator | C-2 merged | assigned | — | **Wire `vendor_connection_auto_disabled` email.** Trigger site is clean at `oauth_refresh_worker:1196` + `:1226` but Cloud Run worker has no fanout dependency wired in `WorkerRuntime`. **Worker pre-recommended Path (b)** — direct outbox enqueue mirror of `VendorLifecycleNotificationDispatcher`, ~400 LoC, faster than Path (a) full fanout (~600 LoC). Confirm path at spawn. Cloud Run worker bootstrap touch (Medium risk). |
| C-2-Del | (new — 2026-05-13 C-2 delete picks) | Claude | Small | Low | auto | C-2 merged | assigned | — | **Delete `vendor_webhook_signature_alert` + `tos_version_updated_notice` templates and their renderer constants.** Operator chose to rely on Cloud Logging alerts (signature) + in-app accept-screen gate (TOS) instead of email. Bundled because both are mechanical deletes (template file + `EmailTemplateIds` constant + matrix row removal + renderer suite still passes). Mirrors the A2.2 PR #540 shape exactly. ~150 LoC delete net. |
| C-3 | C-3 | Codex | Small | Low | auto | B9.1 merged | merged | #579 | Sign-in-security → My Account redirect (decision #7). `SecurityScreen` deleted; side-nav item removed; `/security`, `/sign-in-security`, `/operator-web/sign-in-security` all redirect to My Account security section; recent sign-in activity rendered inside the My Account Security card via existing `WebSecurityGateway`. **-1196 LoC net delete** (huge housekeeping win). 6 files / +688/-1884. 41 tests pass. Pattern B full. Auto-gate; orchestrator merged after clean audit. Triple-safeguard verified (`SecurityScreen` file deleted from master). Audit doc: `docs/_audits/post_codex_wave/pr_579_c_3_sign_in_security_to_my_account_audit.md`. |
| C-4 | C-4 | Codex | Medium | Medium | operator | — | merged | #592 | Master Demo→Live switch — closes 4th HP #2 reader-side carve-out (runtime `demo_mode_state` UI fold, no `kDemoMode` branch, no `demo_*` table, no reader fork). Auth posture mirrors B11.x precedent (role gate → scope → idempotency-key → tenant tx wraps flip + audit + idempotency completion). |
| C-5 | C-5 | Codex | Medium | High | operator | B11.1 merged | merged | #600 | Mobile pointer rows mint B11.1 handoff codes + launch `https://app.forgeflow.app/handoff?code=...&nav=...` via `url_launcher`; Operator Web `/handoff` route redeems via body-only `POST /v1/auth/handoff/redeem` requiring current ID token. CLAUDE.md addendum A1 satisfied (handoff code is the bridge primitive, NOT a JWT/step-up id). Defense-in-depth: redeem requires ID token in HTTP header. Fallback to clipboard for offline/proxy-5xx paths. 11 files / +878/-64. 51 disclosed tests pass. Pattern B exemplary. **Worker disclosed cross-lane B11.2.b test failure on master** (not C-5's fault); investigation agent spawned. |
| C-6 | C-6 | Codex | Medium | Medium | auto | L_A2 merged | assigned | — | Inheritance Tree shared component consumer. Dep updated 2026-05-13 from `B1.a merged` → `L_A2 merged` per operator confirmation: C-6 *consumes* the Inheritance Tree primitive that L_A1 + L_A2 (newly added Lane A rows per B8 Path A pick) are building. Shipping C-6 before L_A2 would leave the consumer with nothing to consume. |
| C-7 | C-7 | Codex | Small | Low | auto | B9.2 merged + C-7a merged | assigned | — | Adaptive 2FA button (R1 pattern). **Data-contract gap unblocker (C-7a) approved by operator 2026-05-13** — `mfa_factors.recovery_codes_viewed_at` column was missing from schema/model/gateway despite being named in `lane_c_parity/03_execution_slices.md`. C-7a ledger row added below; C-7 dependency column updated to require both `B9.2 merged ✓` AND `C-7a merged`. |
| C-7a | (new — 2026-05-13 C-7 prereq, operator approved) | Claude | Small | Low | operator | — | assigned | — | **Prep migration for Codex's C-7 ("Adaptive 2FA button").** Adds `mfa_factors.recovery_codes_viewed_at timestamptz NULL` via `ADD COLUMN IF NOT EXISTS` (additive expand; idempotent). Pure schema-touching prep — mirrors the C-1a → C-1 pattern (PR #599) exactly. No live data + "no business live yet" direction → zero rows touched. Operator approved 2026-05-13 ("yes to all" on the open-decisions slate). Migration joins staging + Production1 apply queue at slot 44. Unblocks Codex's C-7 once merged. |
| C-8 | C-8 | Claude | Medium | Medium | operator | — | merged | #499 | Notification preferences catalog completeness — operator approved 2026-05-12 |
| C-9 | C-9 | Codex | Medium | Medium | operator | C-8 merged | merged | #580 | Mobile in-app inbox renders catalog-backed notification events. Notifications screen resolves presentation from `eventKey` before legacy `type`; safe TOS fallback copy added for template-only rows; catalog icons/accent mapping covers vendor/backfill/audit/shift/star/plan; mobile stays read-only (C-Mobile rule preserved — no preference switches on mobile). 3 files / +363/-80. 19 tests pass. Pattern B full. Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified (`eventKey` rendering live in `notifications_screen.dart` on master, 6 hits). Audit doc: `docs/_audits/post_codex_wave/pr_580_c_9_mobile_inbox_catalog_audit.md`. |
| C-10 | C-10 | Codex | Small | Low | auto | — | merged | #556 | Admin parity copy + read-only-mostly tile labels — F&F-only tiles say "Admin only"; read-mostly support surfaces say "Read-only view" with Operator Web ownership copy; Connected Services labeled as global provider health. 6 files / +185/-24; 145 LoC new pinning tests in `test/admin/admin_parity_copy_test.dart`. Pattern B full both tables. No auth/RLS/schema/proxy/frozen touch. Audit doc: `docs/_audits/post_codex_wave/pr_556_c10_admin_parity_copy_audit.md`. |
| C-11 | C-11 | Claude | Medium | Medium | operator | C-2 merged | assigned | — | Pressure-test inventory under preview |
| C-12 | C-12 | orchestrator | Small | Low | auto | all C-* merged | assigned | — | Final lane-C integration + audit |

## Counts

- Total slices: **59** (+ L_A1 + L_A2 + C-1a added 2026-05-13 — B8 + C-1 prereqs per operator's Path A picks; + C-2-C / C-2-D / C-2-F / C-2-Del / C-7a added 2026-05-13 — C-2 operator picks + C-7 data-contract unblocker)
- Claude owner: **35** (A0/A3.1/A3.2/A3.3/A3.4/A4.1/A4.2/A7.1/A10.1/A11.1/A11.1.b/A11.2/L_A1/L_A2 + B1.a/B1.b/B1.c/B2.1/B2.2/B2.3/B2.4/B8/B11.1/B11.2/B11.2.b + C-1/C-1a/C-2/C-2-C/C-2-D/C-2-F/C-2-Del/C-7a/C-8/C-11) — plus orchestrator A0/C-12
- Codex owner: **22** (A2.1/A2.2/A5+A8/A6.1/A9.1 + B3/B4/B5/B5.b/B6/B7.a/B9.1/B9.2/B9.3/B10.1/B10.2 + C-3/C-4/C-5/C-6/C-7/C-9/C-10)
- Orchestrator owner: **2** (A0 verification probe, C-12 closeout)
- Auto-merge gate: **20** (A11.1.b is auto, observability-only; B2.3/B2.4 are auto, read-only admin observability + operator-web read-only)
- Operator gate: **34** (A3.3 + A3.4 are operator — proxy-touching; L_A1 + L_A2 + C-1a are operator — schema-touching migrations)

## Update protocol

When the orchestrator merges a PR:
1. Find the slice's row.
2. Set `state` = `merged`.
3. Set `pr` = `#<number>`.
4. Move on.

When a PR opens:
1. Find the slice's row.
2. Set `state` = `in-progress`.
3. Set `pr` = `#<number>`.

When a PR's audit completes but operator approval is pending:
1. Set `state` = `audit-pending`.
2. Keep `pr` field.

When a PR is rejected/closed without merge:
1. Set `state` = `back-to-author` (or revert to `assigned` if the executor abandons).
2. Note the reason in a footnote at the bottom of the relevant lane section.

## Change log

| Date | Change |
|---|---|
| 2026-05-12 | Initial ledger — 43 slices, all `assigned`. |
| 2026-05-12 | First wave PRs land. **A6.1 merged** (PR #498 → master `9cdaee1e`). **B1.a / B1.b / C-8 → `audit-pending`** (orchestrator audit clean; awaiting operator approval — see `docs/_audits/post_codex_wave/pr_{499,500,501}_*_audit.md`). B1.a escalates despite `Gate=auto` due to slice-spec planning ambiguity worker correctly surfaced. |
| 2026-05-12 | **Operator approve-all 21:55Z.** Merged: B3 (#502 `fb806262`), B1.b (#500 `427a5510`), C-8 (#499 `5583d5b9`), B1.a (#501 `a6094e9b`). B1.a copy follow-up (Option 1 — rewrite "Other locations…" → "This scope only covers…") applied by orchestrator-fix-by-default in this same PR. Unblocks Codex's held B7.a branch. |
| 2026-05-12 | **A0 verification probe PASS** (orchestrator-owned). B1+B2 hot-fix live on master tip `f1034d0a`; runZonedGuarded + scope-less contract + soak harness all confirmed. Unblocks 9 Lane A slices (A2.1, A2.2, A3.1, A4.1, A5+A8, A7.1, A9.1, A10.1, A11.1). Evidence: `docs/_audits/code_health/a0_b1_b2_post_merge_verification.md`. |
| 2026-05-12 | **B7.a merged + Option A follow-up applied.** Operator approved PR #507 → `4da1a8ef` (Cancel CTA + idempotent revoke + permission narrow). Orchestrator-fix follow-up adds `case 'invite.cancel': return 'Invite cancelled';` to both consumer switches (`web_team_audit_log_gateway.dart`, `auth_operations_gateway.dart`) and updates the parity test mapping. `auth.invite_revoked` label changed to "Invite cancelled" for backwards-compatibility with historic rows. 12+1 tests pass. |
| 2026-05-13 | **A10.1 merged** (PR #523 → `d789c22c`). 9 `git mv` renames + 5 harness import-path updates + .gitignore retarget. Dropped stale `settings_permission_explainer_test.dart` entry from `KNOWN_FAILING_TESTS.md` (verified deleted in W3.A sweep commit `22db522a`). Added entry for 2 pre-existing p3c closure-registry assertion failures (NOT regressions). Audit doc: `docs/_audits/post_codex_wave/pr_523_a10_1_test_consolidation_audit.md`. |
| 2026-05-13 | **A11.1 → `audit-pending`** (PR #522). Production session-record incomplete gauge wired at `POST /v1/auth/session/login`. Observability-only (no PII labels, defensive try/catch, identical response bytes, optional back-compat param). B11.1 auth-handoff routes verified untouched. Proxy-touching → operator gate. Audit doc: `docs/_audits/post_codex_wave/pr_522_a11_1_session_record_gauge_audit.md`. |
| 2026-05-13 | **A11.1 merged** (PR #522 → `1ddce436`). Operator approved after plain-English summary. Gauge class `SessionRecordIncompleteGauge` confirmed live on master at `tool/advisor_proxy/advisor_proxy.dart:6969`. Unblocks A11.2 (soak harness durable extensions). |
| 2026-05-13 | **A10.1 doc-hygiene followup** (PR #528 → `fefa6e5b`). 15 forward-looking `test/load/pressure/` refs in `docs/_audits/code_health/a11_soak_harness_durable_kit.md` updated to `test/pressure/`; 1 historical state-comparison row on line 43 preserved. The other 5 docs the A10.1 worker flagged turned out to be historical "as-observed" records that should stay as-is — analyzed and left alone. |
| 2026-05-13 | **A7.1 merged** (PR #526 → `183ab38c`). Canonicalized framework path: chose `docs/frameworks/<NAME>.md` as the single canonical path (verified by Glob that all 5 framework files physically live there; ZERO copies at docs root means prior `docs/<NAME>FRAMEWORK.md` refs were broken links). Fixed 7 refs across 3 active docs; rewrote `docs/frameworks/README.md` as a correct 5-framework index. Worker disclosed canonical-hook installer fix + rebase note (positive honesty patterns). |
| 2026-05-13 | **A5+A8 → `audit-pending`** (PR #527). Tooling/lint/convention only — ZERO production migrations applied. Adds `db/migrations/post_deploy/` convention dir + README (expand-only top-level, contract-phase post-deploy), `tool/audit_logs_update_lint.dart` (UPDATE allowlist lint), `tool/audit_logs_update_allowlist.txt` (1 entry: historical `business_date` backfill, A5-approved retrospectively), `--require-expand-contract` mode on migration drift scanner (grandfathered to B11.1's migration `202605131030_b11_1_auth_handoff_codes.sql`). CI wired in `repo-lints` job. Schema-touching → operator gate. Audit doc: `docs/_audits/post_codex_wave/pr_527_a5_a8_audit_log_guardrails_audit.md`. |
| 2026-05-13 | **A9.1 → `audit-pending`** (PR #530). `lib/data/` rehome — 3 frozen-legacy files moved (app_defaults + cross_axis_pair_catalog → `lib/domain/constants/`; mock_integration_replay_seed → `lib/dev/`). 72 files / +90/-90 (mostly import-path updates). 3 active contract docs updated (demo_mode + phase_7_58_primary_driver + phase_7_61_driver_key). 198/198 targeted tests pass. `lib/data/` will be empty post-merge (delete-only doctrine fully honored). Worker disclosed internal A9-F1 send-back catch (positive). Frozen-surface + demo-mode-parity sensitivity → operator gate. Audit doc: `docs/_audits/post_codex_wave/pr_530_a9_1_lib_data_rehome_audit.md`. |
| 2026-05-13 | **A5+A8 merged** (PR #527 → `f597252a`). Operator approved after plain-English summary. `db/migrations/post_deploy/`, `tool/audit_logs_update_lint.dart`, `tool/audit_logs_update_allowlist.txt` all confirmed live on master. Grandfather cutoff anchored to B11.1's migration — existing history intact; new gate enforces starting with the next migration. |
| 2026-05-13 | **A9.1 merged** (PR #530 → `5330fcfd`). Operator approved after plain-English summary. `lib/data/` confirmed EMPTY on master post-merge (delete-only doctrine fully honored). 3 files at new homes verified: `lib/dev/mock_integration_replay_seed.dart`, `lib/domain/constants/app_defaults.dart`, `lib/domain/constants/cross_axis_pair_catalog.dart`. |
| 2026-05-13 | **A3.1 → `audit-pending`** (PR #533). Monolith seam-map + bleed-stop CI lint. `advisor_proxy.dart` inspected READ-ONLY (blob SHA verified unchanged). Adds `tool/advisor_proxy_size_lint.dart` (176 LoC, testable façade), 258-line seam map at `docs/_audits/code_health/a3_advisor_proxy_seam_map.md` (12 clusters with file:line ranges, all spot-checked correctly), +45 append to `proxy_split_plan.md`. CI lint runs cleanly: 18,871 / 19,071 / 200 headroom. **Orchestrator-applied conflict resolution**: rebased onto current master (A5+A8 had renamed lint-chain step name in ci.yml); pure mechanical resolution, no semantic change to worker intent. Force-pushed with `--force-with-lease`. Audit doc: `docs/_audits/post_codex_wave/pr_533_a3_1_monolith_seam_map_audit.md`. |
| 2026-05-13 | **A3.1 merged** (PR #533 → `80148903`). Operator approved after plain-English summary. `tool/advisor_proxy_size_lint.dart` + seam-map doc confirmed live on master. Bleed-stop ceiling 19,071 lines + ratchet rule now in force. Unblocks A3.2 (bare-catch tail chunk 1 of 3). |
| 2026-05-13 | **A11.2 → `audit-pending`** (PR #537). Soak harness durable extensions: fd watcher (Linux), heap-snapshot uploader (storage-agnostic, GCS impl, env-gated), p3c CLI flags. 6 files / +1876/-5. 31/31 new tests pass. Zero pubspec deps. **Operator-decision flagged**: GCS vs Azure backend (interface allows easy swap). Recommendation: defer backend choice; merge as-is. Audit doc: `docs/_audits/post_codex_wave/pr_537_a11_2_soak_harness_extensions_audit.md`. |
| 2026-05-13 | **Doc-hygiene PR #539 audit-pending**. Orchestrator-spawned sub-agent trimmed 41 archive files (3 in `archive/internal/`, 38 in `archive/_execution/` — all closed-PR proofs / one-off snapshots / "CLOSED" dispatch plans, all with zero references outside archive/audits per agent's per-file grep) + 1 CLAUDE.md edit (replaced dead `docs/BETWEEN_SPRINT_AUDIT_PROMPT.md` ref with prose). Agent restraint: 0 memory consolidations, 0 phase doc moves, 0 active-doc updates — all assessed correctly. Audit doc: `docs/_audits/post_codex_wave/pr_539_doc_hygiene_trim_audit.md`. |
| 2026-05-13 | **A2.2 → `audit-pending`** (PR #540). Email pipeline wire-or-delete — Codex deleted 2 dormant SendGrid templates + registry entries. Verified zero production consumers; Firebase action-link email is the live path. 21/21 tests pass. Audit doc: `docs/_audits/post_codex_wave/pr_540_a2_2_email_wire_or_delete_audit.md`. |
| 2026-05-13 | **PR #542 audit-pending** (NOT a wave slice). Gates Apple Platform Verification CI workflow to `workflow_dispatch` only — removes auto-run on push/PR. Diagnoses 5-day CI-red-baseline as GitHub Actions spending-cap exhaustion cascaded by macOS-15 runners (10× Linux billing). 1 file / +9/-4. Operator action items: clear billing cap, drop required-status-check on Apple workflow if currently set. Audit doc: `docs/_audits/post_codex_wave/pr_542_apple_ci_gate_audit.md`. |
| 2026-05-13 | **PR #542 merged out-of-band by operator** at `714686ce` (01:56Z). Apple CI auto-run now gated to `workflow_dispatch` only. |
| 2026-05-13 | **A11.2 merged** (PR #537 → `8463d56b`). Operator approved with binding follow-up to swap GCS → Azure Blob (logged in `docs/POST_HARDENING_FOLLOWUPS.md` P1 section). GCS env vars MUST stay unset until the swap lands. fd watcher + p3c CLI flags are independent of the swap and remain live. |
| 2026-05-13 | **A2.2 merged** (PR #540 → `7ff5e3bc`). Operator approved. 2 dormant SendGrid templates + registry entries deleted; `operator_invite_first_admin.md` preserved as admin SendGrid test fixture. Firebase action-link email continues to be the live path for password reset + admin invite (no operator-visible behavior change). |
| 2026-05-13 | **Doc-hygiene PR #539 merged** at `d1c2e167`. Operator approved. 41 archive files deleted (3 in `archive/internal/`, 38 in `archive/_execution/`, all closed-PR proofs / one-off snapshots / "CLOSED" dispatch plans, zero-reference verified). 1 CLAUDE.md edit (dead `docs/BETWEEN_SPRINT_AUDIT_PROMPT.md` ref → equivalent prose). Git history preserves all deleted content. |
| 2026-05-13 | **B9.2 → `audit-pending`** (PR #547). My Account 4-card IA consolidation (Profile/Security/MFA/Active Sessions). Operator-web only (5 files, all in `lib/operator_web/**`); uses existing `/v1/auth/sessions` + `/v1/auth/session/revoke` routes; client-side `_requireFreshMfaToken` gate decodes ID-token `auth_time` claim. Current session excluded from revoke with empty-list snackbar + "This device" badge. Audit-log link on every card per IA spec. Cross-lane note (honest): server-side RFC 9470 step-up on revoke route is OUT of B9.2 scope; belongs with B11.2. 34/34 tests pass; `dart analyze` clean. Auth-adjacent (revoke flow) → operator gate. Audit doc: `docs/_audits/post_codex_wave/pr_547_b9_2_my_account_active_sessions_audit.md`. |
| 2026-05-13 | **B9.2 merged** (PR #547 → `cc076652`). Operator approved conditional on B11.2 server-side step-up being planned — confirmed B11.2 row (line 66) explicitly scopes RFC 9470 step-up challenge on sensitive routes. Triple-safeguard: is-ancestor confirmed; new file `lib/operator_web/account/operator_web_account_actions.dart` present on master; revoke reason `my_account.sign_out_other_sessions` confirmed in `web_account_gateway.dart` on master. Unblocks B9.3 (adaptive 2FA button) + C-7 (mobile parity). |
| 2026-05-13 | **B11.2 → `audit-pending`** (PR #550). Auth-critical + schema + RLS + proxy → operator gate (4 triggers). Ships scaffold-only: new `auth_step_up_challenges` table (operator-scoped, wrapper-only RLS via `app_current_operator()`, operator-leading B-tree indexes with partial filter on `consumed_at IS NULL`, TTL hard ceiling 15min, base64-url id shape CHECK), pure-function `StepUpPolicy`, `StepUpChallengeRouter` with oracle-safe failure classification (unknown id → 401 not 410), RFC 9470 header builder, 4 abstract seams (gateway + audit sink). 27/27 tests pass; advisor_proxy_size_lint clean (18,871/19,071 headroom 200). **Material finding**: 0/4 of slice spec Files row touched (`advisor_proxy.dart` wiring, `lib/auth/fresh_mfa_resolver.dart`, `mfa_freshness_redirect_listener.dart`, `lib/operator_web/auth/step_up_challenge_handler.dart` — all deferred). Three deferrals disclosed in PR body but NOT logged in POST_HARDENING_FOLLOWUPS.md or as a B11.2.b ledger row. Auth posture on master is unchanged after merge — B9.2's conditional approval is satisfied in letter (B11.2 row → merged) but not in spirit (server-side gap still open) until B11.2.b wiring lands. Audit doc: `docs/_audits/post_codex_wave/pr_550_b11_2_step_up_challenge_audit.md`. Operator decision needed: Option A (approve + amend ledger to add B11.2.b + log deferrals), Option B (send back for full vertical), Option C (re-scope into slice family). Recommendation: Option A. |
| 2026-05-13 | **A4.2 → `audit-pending`** (PR #552). Performance fixes — all 3 A4.1 ranked recs landed in one slice: R1 (Postgres pool default 4→20 — fallback-only, prod env-pinned to 20 already), R2 (4 shift_dashboard 30s `Timer.periodic` → 1 shared `_ShiftDashboardTicker` ValueNotifier; same cadence, identical render output, disposal hygiene preserved), R3 (`healthProducerConcurrency` env-resolved via `resolvePostgresMaxConnectionsPerPool()`). 11 files / +608/-288 (3 prod + 5 comment-sweep + 3 tests). All disclosed test runs clean (3 new ticker tests + 8 existing daypart + 9 postgres_executor + 15 health-check-store + 60 broader regression). Baseline-snapshot doctrine honored (pre-existing 2 widget-test failures verified NOT introduced). Infra-touching (Postgres pool default) → operator gate. **P3 nit**: Pattern B drift — 2nd occurrence from this Claude session (counter 2/3). Audit verdict: approve-for-merge, low-risk in practice. Audit doc: `docs/_audits/post_codex_wave/pr_552_a4_2_performance_fixes_audit.md`. |
| 2026-05-13 | **A4.2 + B11.2 merged** (PRs #552 → `f9b0c1f0`, #550 → `42c35f24`). Operator approved both 2026-05-13 02:46Z. B11.2 approved as Option A (scaffold-only + B11.2.b for wiring) per converging audit recommendations. Triple-safeguard: both merges ancestor-of-master; `kPostgresDefaultMaxConnectionsPerPool = 20` confirmed on master; B11.2 migration + routes files present on master. **Added B11.2.b row** (new — operator decision 2026-05-13) to capture: (1) per-route wiring into proxy handlers, (2) `RepositoryStepUpChallengesGateway` Postgres binding, (3) `lib/auth/` + `lib/operator_web/auth/` client adapters, (4) deep-audit P1 fix for B9.2 `_requireFreshMfaToken` clock-skew flaw. Three deferrals folded into the new ledger row — POST_HARDENING_FOLLOWUPS not bumped (followups doc reserved for orphan items without a clear slice home). |
| 2026-05-13 | **Deep audit P1 deposit** (per operator-approved game plan 2026-05-13). Wave completion deep audit produced 5 P1 findings; deposited to ledger per new POST_HARDENING_FOLLOWUPS-hygiene rule: (a) **B1.c row added** for 4-site `actorKind` peer-bug in `proxy_bootstrap.dart` admin gateways (pricing/corpus/graph/feature-flags); (b) **A11.1.b row added** for `SessionRecordIncompleteGauge` consumer wiring (observability-only, auto gate); (c) **B11.2.b** already captures B9.2 clock-skew flaw + step-up bypass; (d) **B11.1 idempotency-store convention** → POST_HARDENING_FOLLOWUPS (no clear immediate slice home); (e) doc-drift P2s + nits → batched followups line. Counts: Total 44 → 46; Claude 21 → 23; Operator-gate 27 → 28; Auto-merge 17 → 18. Source: `docs/_audits/post_codex_wave/wave_completion_deep_audit_2026_05_13.md`. |
| 2026-05-13 | **C-10 merged** (PR #556 → `ff1772b6`). Codex admin parity copy slice — F&F-only "Admin only" badge, "Read-only view" badge with Operator Web ownership note for read-mostly tiles, Connected Services framed as global provider health. 6 files (+185/-24); 145 LoC new pinning tests at `test/admin/admin_parity_copy_test.dart` (48/48 passing). Pattern B full both tables. Auto-gate (no auth/RLS/schema/proxy/frozen touch); orchestrator merged after clean audit per orchestrator-auto-merge-after-audit memory rule. Triple-safeguard verified (`"Admin only"` copy live on master). Audit doc: `docs/_audits/post_codex_wave/pr_556_c10_admin_parity_copy_audit.md`. |
| 2026-05-13 | **B5 → `audit-pending`** (PR #557). Codex admin access-control sweep. Three deliverables: (1) business-timing route role split — GET admits {super_admin, ff_support}; writes (POST/PUT/PATCH/DELETE) narrowed to {super_admin only}. (2) Dispatcher route-ordering bug fix at `advisor_proxy.dart:14931` — early-returns false from `_isAdminOperatorOrLocationOperation` when `AdminBusinessTimingRouter.matches`, preventing operator/location matcher from swallowing business-timing routes. Load-bearing — without it the role split has no effect. (3) `admin_reason` added to 7 HTTP operator/location mutations via `_withAdminReason()` helper (`onboard`, `patch_operator`, `suspend`, `reactivate`, `add_location`, `patch_location`, `remove_location`); pairs with earlier server-side enforcement migrations to make admin audit log end-to-end honest. **Permission-key completeness half deferred** — frozen `lib/auth/**` intentionally NOT touched; catalog amendment proposed in slice-owned follow-up doc `docs/_execution/lane_b_features/05.5_catalog_followup.md`. Same scope-split pattern as B11.2/B11.2.b yesterday. 4 files (+440/-19); 339 LoC new dispatcher tests in `test/advisor_proxy_test.dart` covering ff_support read (200), ff_support write denial (403), super_admin write (201) + audit-sink admin_reason flow. 63 disclosed local test runs. Pattern B full both tables. Auth-critical + proxy-touching → operator gate. Audit doc: `docs/_audits/post_codex_wave/pr_557_b5_admin_access_control_audit.md`. |
| 2026-05-13 | **Doc-drift cleanup merged** (PR #558 → `6d7d8bb4`). Orchestrator-authored via worktree agent. 3 mechanical fixes to `docs/POST_HARDENING_FOLLOWUPS.md`: (a) Resolution line added to P2 kDemoMode reader-side carve-out entry (already ✅ FIXED via PR #417); (b) P3 docs/_execution retirement window heading flipped to "opened … ✅ FIRST SWEEP COMPLETE" with PR #539 → `d1c2e167` provenance (41 archive files swept); (c) advisor_proxy.dart line count refreshed 16,949 → 18,871 per A3.1 measurement + bleed-stop ceiling 19,071 + 200 headroom context. 2 files (+35/-4). Audit doc: `docs/_audits/post_codex_wave/followups_doc_drift_cleanup_2026_05_13.md`. |
| 2026-05-13 | **B5 merged** (PR #557 → `1f51f4f1`). Operator approved 03:22Z as Option A (scaffold-style scope split: ship access-control + dispatcher fix + admin_reason now; defer frozen-catalog amendment to B5.b). Triple-safeguard verified: `kAdminBusinessTimingRoles` symbol confirmed in `tool/advisor_proxy/advisor_proxy.dart` on master; `admin.operator_location.suspend` admin_reason string confirmed in `lib/admin/services/operator_location_admin_gateway.dart` on master. **Added B5.b row** (new — operator Option A) to capture the deferred catalog amendment: 2 new permission keys (`account.configure`, `business_timing.configure`), seed grants migration, and 3 screen literal→constant swaps per the slice-owned `docs/_execution/lane_b_features/05.5_catalog_followup.md`. Counts: Total 46 → 47; Codex 21 → 22; Operator-gate 28 → 29. Auto-merge count unchanged. |
| 2026-05-13 | **B1.c merged** (PR #561 → `5a89f4ca`). Operator approved 03:38Z (recommendation Option A — approve as-is). Claude lane salvaged PR from relaunch — orchestrator inspected salvage path (clean diff, no drift artifacts). 4 admin gateway `_audit` helpers flipped to `actorKind: 'forge_admin'` in `tool/advisor_proxy/proxy_bootstrap.dart` (pricing/corpus/graph/integrations); 4 audit-chain integrity tests in `test/advisor_proxy_bootstrap_test.dart`; deep-audit doc finding #1 marked RESOLVED. Triple-safeguard verified (`forge_admin` appears 20× across `proxy_bootstrap.dart` on master). **Pattern B violation 3rd occurrence from Claude lane session** (PRs #550, #552, #561) — prompt-drift flag lit; substance honest, template-format drift only. Audit doc: `docs/_audits/post_codex_wave/pr_561_b1_c_actorkind_peer_bug_sweep_audit.md`. |
| 2026-05-13 | **A3.2 merged** (PR #563 → `be7010dc`). Operator approved 03:38Z (recommendation Option A — accept cluster-aligned chunking 12+16+16 + pure-refactor reasoning for test gap). Claude lane salvaged PR from relaunch (same session as B1.c). 12 bare-catch sites typed across clusters 2-3 + early cluster 4 of A3.1 seam map (JWT verification, JWKS, signature, ProxyUsageGuard); 1 intentional retention at line 2416 (`Platform.environment` `UnsupportedError` swallow — `Error` not `Exception`, narrowing would break fallback). `on Object` deliberately never used. Bleed-stop lint 18,871 → 18,899 (+28; headroom 172). Decomposition-doc registry section formalizes A3.2/A3.3/A3.4 cluster ranges. **Pattern B violation 4th occurrence from Claude lane session** (PRs #550, #552, #561, #563) — confirms systemic prompt drift; operator-approved fix is a one-line reminder in next Claude lane prompt. Audit doc: `docs/_audits/post_codex_wave/pr_563_a3_2_bare_catch_typing_chunk_1_audit.md`. **Unblocks B11.2.b** — all 3 deps now merged (B11.2 ✓ + A3.2 ✓ + A4.2 ✓). |
| 2026-05-13 | **A3.3 + A3.4 rows added** to formalize the cluster-aligned chunking from A3.2's PR #563 decomposition-doc registry section. Both were implicit in A3.2's slice spec ("3 chunks total: A3.2/A3.3/A3.4") but never had ledger rows. Added now so the fresh Claude lane loop-mode session can pick A3.3 as the natural follow-on. Same shape: Claude owner, M/M, operator gate (proxy-touching), dep chain A3.3 → A3.2 merged, A3.4 → A3.3 merged. Counts: Total 47 → 49; Claude 23 → 25; Operator-gate 29 → 31. Also part of Claude lane restart housekeeping per operator decision to restart that session in loop mode (Pattern B 4/4 drift resolved by fresh session + saved master-prompt at `~/.claude/.../memory/handoff_prompt_claude_lane_loop_mode.md`). |
| 2026-05-13 | **Final housekeeping sweep** (PR #569 → `e0c3ab4a`). Staged repo for the Claude lane restart. 7 files touched (zero production code, zero ledger, zero CLAUDE.md): `docs/_indices/README.md` rewritten; `CLAUDE_LANE_INDEX.md` + `CODEX_LANE_INDEX.md` trimmed (removed duplicative State column; refreshed lane scopes for today's row additions; expanded cross-references for newly-merged lanes); `CLAUDE_HANDOFF_PROMPT.md` + `CODEX_HANDOFF_PROMPT.md` refined (3-block worker brief + canonical-hooks step + banned items + banned imports + sensitive-path flag + salvage discipline + Pattern B exemplars); new `docs/_audits/post_codex_wave/README.md` audit doc index covering all 41 audit docs. Memory file `handoff_prompt_claude_lane_loop_mode.md` demoted to thin pointer since in-repo `CLAUDE_HANDOFF_PROMPT.md` is now canonical. Audit doc: `docs/_audits/post_codex_wave/final_housekeeping_sweep_2026_05_13.md`. |
| 2026-05-13 | **B9.3 merged** (PR #568 → `c935e436`). Codex adaptive 2FA card — 4-state machine reading server-backed factor/removal state + client-side clock-skew handling in operator-web auth source. 7 files / +1413/-51 (scope expanded but coherent; ledger S/L estimate retained). 54 disclosed local test passes. Pattern B both tables present. Auto-gate; orchestrator merged after clean audit per orchestrator-auto-merge-after-audit memory rule. Triple-safeguard verified. **Complements B11.2.b** — B9.3 ships client-side clock-skew, B11.2.b will ship server-side at `web_account_gateway.dart` + RFC 9470 challenge wiring. Cross-lane note in PR body is honest about the division. Audit doc: `docs/_audits/post_codex_wave/pr_568_b9_3_adaptive_2fa_card_audit.md`. **Unblocks C-7** — mobile adaptive 2FA button parity (R1 pattern). |
| 2026-05-13 | **A11.1.b merged** (PR #571 → `ce0d5a62`). **First Claude lane PR post-loop-mode restart — Pattern B drift trend reversed from 4/4 pre-restart → 0/2 post-restart.** Wave deep audit finding #2 fix: `SessionRecordIncompleteGauge.snapshot` wired into `/health` envelope via new `sessionRecordIncompleteCountProducer` in the `infra` family. Bootstrap hoist closes a latent disjoint-counter bug (snapshot side was observing a different instance from the increment side). Defensive projection (null → `not_wired`, throw → `producer_error`, empty steady state distinguishable from not-wired). PII-safety asserted (no `user_id`, `operator_id`, `location_id`, `session_id`, `email`, `ip` in metric labels). 7 files / +463/-4. Bleed-stop lint 18,904 → 18,957 (+53; headroom 114). 111 disclosed local test passes. Pattern B exemplary (worker 14L + executor 14L with file:line citations; worker undercounted the `inMemoryBreakerStates` mirror sites — said 4, actual 6, honest). Operator approved 2026-05-13 — auto-gate carve-out for observability-only proxy slices confirmed going forward. Triple-safeguard verified (`sessionRecordIncompleteCountProducer` 2 hits + `sessionRecordIncompleteSnapshot` 5 hits on master). Audit doc: `docs/_audits/post_codex_wave/pr_571_a11_1_b_session_record_gauge_consumer_audit.md`. |
| 2026-05-13 | **A3.3 merged** (PR #572 → `74814567`). Bare-catch chunk 2/3 continuation of A3.2 Option A. 5 sites typed in clusters 4-8 (came in lower than ~16 estimate; A3.4 revised ~16 → ~22 from fresh grep). All 5 sites narrowed to `on Exception catch (_)` with 5-line rationale comments naming concrete throw classes (`PgException`, `TimeoutException`, `IOException`, etc.); `Error`-class throws propagate per addendum C4. Retentions preserved correctly: line 2426 (`Platform.environment` swallow — `UnsupportedError` is `Error` not `Exception`); line 7024 (`SessionRecordIncompleteGauge.observe()` — owned by A11.1.b). 2 files / +76/-11. Bleed-stop lint 18,904 → 18,934 (+30; headroom 137). 248 existing tests pass (no backfill per A3.2 Option A precedent). Pattern B exemplary (worker 14L + executor 14L). Operator approved 2026-05-13 as Option A continuation. Triple-safeguard verified (10 `on Exception catch (_)` sites on master in advisor_proxy.dart). Audit doc: `docs/_audits/post_codex_wave/pr_572_a3_3_bare_catch_typing_chunk_2_audit.md`. **Unblocks A3.4** — bare-catch chunk 3/3 (clusters 9-12, ~22 sites). |
| 2026-05-13 | **B5.b merged** (PR #573 → `414cd37e`). Codex catalog tri-mirror amendment — B5 Option A deferred half. Two new permission keys `account.configure` + `business_timing.configure` landed in tri-mirror across (a) `docs/contracts/auth_permission_key_catalog.md`, (b) frozen `lib/auth/permission_keys.dart` constants + `allKeys` set, (c) additive migration `202605131500_b5_b_catalog_tri_mirror.sql` (idempotent inserts with `ON CONFLICT DO NOTHING`; grants scoped to super_admin + operator_owner + operator_admin only). 3 operator-web settings screens swap string literals → `PermissionKeys.*` constants. Permission-key lint surface expanded to catch future tri-mirror drift; new `test/auth/permission_catalog_b5b_test.dart` (139 LoC) asserts the three sides agree. 16 files / +336/-81. 54 disclosed local test passes. Pattern B full. Operator approved 2026-05-13 via delegation ("approve codex once audited and you are satisfied"). Triple-safeguard verified (`accountConfigure` + `businessTimingConfigure` constants live in `permission_keys.dart` on master; migration file present at expected timestamp path). Audit doc: `docs/_audits/post_codex_wave/pr_573_b5_b_catalog_tri_mirror_audit.md`. **Migration pending apply to staging + Production1** — `runbooks/phase_9_production1_migration_apply_runbook.md` updated. |
| 2026-05-13 | **B10.1 merged** (PR #576 → `ebb18708`). Codex `vendor_applicability` plumbing — temporal table + RLS + admin/operator routes + gateway clients + schema-validated JSONB metadata. Schema is exemplary: 6 indexes with operator-leading split (F2 fix); RLS via `app_current_operator()` wrapper (no bare `current_setting()`); temporal write semantics (no DELETE — `effective_until` sets close); defensive format constraints on `setting_kind`/`setting_key`/`vendor_slug`; cascade on operator delete. Routes: super_admin-only admin gate (`kFfVendorApplicabilityAdminRoles`); required `admin_reason` at gateway-command DTO level (F1 fix); required `Idempotency-Key` with max-length 200 (400 on missing/too-long). JSONB `metadata` schema-validated by `lib/services/settings/applicability_metadata_schemas.dart` (334 LoC) to prevent EAV escape hatch. 20 files / +3680/-149 (140 migration + 441 repository + 332 admin gateway + 197 operator-web gateway + 477 proxy routes + ~1463 tests). 40 disclosed local test passes. Two send-back fixes (F1 required reason + F2 split indexes) applied cleanly. Pattern B full (worker 14L + executor 14L). Operator approved 2026-05-13 via break-time delegation ("approve once audited and you are satisfied; do not wait on me"). Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_576_b10_1_vendor_applicability_audit.md`. **Migration pending apply to staging + Production1**. **Unblocks B10.2** (vendor applicability UI). |
| 2026-05-13 | **C-3 + C-9 + A3.4 merged in 3-PR batch** (PRs #579 → `df060dc2`, #580 → `30bb4bea`, #581 → `80248812`). Operator break-time delegation; all 3 audited clean by orchestrator. **C-3 (#579, Codex)**: Sign-in-security → My Account fold. `SecurityScreen` deleted; side-nav item removed; 3 legacy routes redirect to My Account security section; recent sign-in activity rendered inline. -1196 LoC net delete. **C-9 (#580, Codex)**: Mobile inbox catalog rendering. `eventKey` resolution preferred over legacy `type`; TOS fallback copy; mobile read-only preserved. **A3.4 (#581, Claude)**: Bare-catch chunk 3/3 — 26 sites typed; 25 to `on Exception catch (_)`, 1 to `on FormatException` (Base64Decoder). **A3.x cluster CLOSED** — 43 typed + 2 retained + 1 B10.1 carry-forward = 45 sites accounted for. A3.4 honest disclosures: self-disclosed `git stash` incident cleanly recovered; pre-existing master size-lint regression from B10.1's +400 lines (master 19,458 / 19,071 ceiling — A3.4's +78 was inside ratchet at base); 5 pre-existing test failures in `admin_cors_bootstrap_test.dart` reproducible on master pre-A3.4 (probably B10.1 fallout); 1 B10.1 carry-forward bare-catch at line 14109. **Orchestrator follow-ups owed** (bundle 33+): raise `kAdvisorProxyMaxLines` ceiling; fix B10.1 carry-forward bare-catch at the new post-A3.4 line; investigate admin_cors_bootstrap_test failures. Audit docs: `pr_579_c_3_*`, `pr_580_c_9_*`, `pr_581_a3_4_*`. |
| 2026-05-13 | **Orchestrator Bundle 33 merged** (PR #583 → `781b84c0`). B10.1 fallout cleanup per A3.4 worker disclosures: (a) `kAdvisorProxyMaxLines` ceiling raised 19,071 → 19,600 (B10.1's +400 lines without ceiling raise pushed master 387 over); (b) B10.1 carry-forward bare-catch at line 14109 typed to `on Exception catch (_)` with 7-line rationale comment (same idiom as A3.4's 26 sites). 3 files / +79/-2 (incl. audit doc). Lint clean post-merge (19,543 / 19,600, headroom 57). Audit doc: `docs/_audits/post_codex_wave/orchestrator_bundle_33_b10_1_fallout.md`. **Still deferred**: `admin_cors_bootstrap_test.dart` 5 pre-existing failures needing separate investigation. |
| 2026-05-13 | **B2.1 merged** (PR #584 → `67404646`). Claude lane F&F-wide versioned Default Role catalog with full production wiring. **Build-Toward-Production discipline honored**: worker 1 deferred production sink "to B2.2" → executor sent back citing CLAUDE.md "Build Toward Production" doctrine → worker 2 implemented `ProductionDefaultRoleCatalogAuditSink` + bootstrap → executor inlined 1-line `main.dart` threading per orchestrator-fix-inline default. End-to-end production posture functional: bootstrap → bindings → main.dart routeRequest → dispatcher → router → production sink → audit row (hash-chained `audit_logs` via INSERT-only). Bootstrap test pins non-null + production-sink + not-noop (3 expectations). 18 files / +3437/-11. 71 new tests pass. Pattern B exemplary (worker 14L + executor 14L; executor adds production-wiring-completeness + cross-operator-admin-posture lenses). Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_584_b2_1_default_role_catalog_audit.md`. **Migration pending apply to staging + Production1**. **Bleed-stop ceiling raised 19,600 → 19,700** in same bundle (Option A; B2.1 added +85 to advisor_proxy.dart — necessary dispatch envelope; master post-merge 19,628, headroom 72). **Unblocks B2.2** (admin editor UI). |
| 2026-05-13 | **B11.2.b merged** (PR #586 → `6ed8641a`). **Auth-critical, HIGH risk — most consequential auth slice of the wave.** Claude lane RFC 9470 step-up challenge wiring + production binding + client adapters + B9.2 P1 clock-skew fix — all 4 scopes shipped together per Build-Toward-Production (no scaffolding, no main.dart-deferral anti-pattern). **Auth posture change**: "any verified bearer admitted on sensitive routes" → "fresh `auth_time` required on **20 sensitive routes**; one-shot step-up challenge if stale." Single choke-point at `routeRequest` head with registry-driven `kStepUpSensitiveRoutes` filter (20 specs verified by executor grep — worker disclosure of 20 routes vs estimated 14 is better-than-estimated, fully transparent). New `StepUpChallengesRepository extends OperatorScopedRepository<T>` mirrors B11.1's pattern exactly: emit/consume via `withTenant`, `lookupForReplayCheck` via `withSystem` ONLY (oracle-safe — collapses cross-operator / wrong-route / wrong-user into single 401). `lib/auth/permission_keys.dart` **UNTOUCHED** (frozen catalog respected). Addendum A1 (Step-Up-Challenge-Id is HEADER, NEVER URL) explicitly enforced via dedicated `URL-param-ignored` test. Clock-skew window ±60s (RFC 7519 §4.1.4 leeway) with boundary tests pinned. Audit rows: `auth.step_up_challenge.required` + `auth.step_up_challenge.consumed` via INSERT to hash-chained `audit_logs`; challenge_id stored as SHA-256 hex (never raw). 16 files / +2854/-12. 58 new + 67 pre-existing sibling tests pass. Pattern B exemplary (worker 14L + executor 14L; executor adds choke-point-pattern-correctness + oracle-safe-replay-classifier lenses). Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_586_b11_2_b_step_up_wiring_audit.md`. Bleed-stop lint clean: 19,688 / 19,700 (headroom 12; inside Bundle 34's ratchet). **Closes B11.2 scaffold's production deferral** + **bundles B9.2 clock-skew P1 fix**. **Unblocks C-7** (mobile parity). |
| 2026-05-13 | **C-6 dep updated** (operator confirmation). C-6's dependency column changed from `B1.a merged` → `L_A2 merged`. Rationale: C-6 ("Inheritance Tree shared component consumer") *consumes* the Inheritance Tree primitive that L_A1 + L_A2 are building per operator's B8 Path A pick. Shipping C-6 before L_A2 merges would leave the consumer with nothing real to consume. Original `B1.a merged` dep was correct at slice spec time but did not anticipate the L_A1/L_A2 promotion in Bundle 40. Pure ledger-row edit; no code/doc state change. |
| 2026-05-13 | **B2.3 merged** (PR #603 → `746fe436`). Claude lane blast-radius endpoint closes B2.2 Gap 1. New `GET /v1/admin/auth/role-catalogs/blast-radius?version_id=<uuid>` returns `{operator_count, location_count, user_count, version_id}`. B2.2 publish dialog now renders the specced "Affecting N businesses, M locations, P users currently following version X" numerics; plain-English fallback preserved for error/zero/genesis states (error chip surfaces proxy code). `runAsSystem` admin-pool BYPASSRLS for F&F-global cross-operator aggregate per CLAUDE.md "RLS-Ready Schema" (`default_role_catalog_versions` has no `operator_id` column). Single CTE pattern: `with pinned_operators as (select operator_id from public.operators where default_role_catalog_version_id = $version_id::uuid)` then three counts off one snapshot — concurrent operator-flip cannot create partial-result skew. Role gate `kDefaultRoleCatalogAdminReadRoles = {super_admin, ff_support}` matches B2.1 GET posture exactly. `versionIdQueryParam` threaded as default-null parameter — backwards-compat preserved for existing B2.1 callsites. 9 files / +1597/-42. 31 new tests (19 route + 8 repo + 4 dialog) + 42 regression-checked clean. Pattern B exemplary (worker 14L + executor 14L; executor adds CTE-single-round-trip-correctness + bleed-stop-ratchet + honest-gap-discipline lenses). Bleed-stop lint clean: 19,803 → 19,805 (+2 LoC dispatch thread only; route logic in `admin_default_role_catalog_routes.dart`); ceiling 19,900; headroom 95. Operator approved 2026-05-13 via "no business live yet" direction (Bundle 39 ledger row addition). Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_603_b2_3_blast_radius_endpoint_audit.md`. **Two honest disclosures** (non-blocking): (a) 5 pre-existing test failures in `test/proxy/` NOT introduced by B2.3 — investigation candidate for closeout; may be wall-clock time-bombs (B11.2.b pattern) or stale snapshots (admin_cors_bootstrap_test pattern); (b) user-count caveat — `public.users` has no soft-delete column, so count is total users-on-file for pinned operators, not active-only. Documented in repo dartdoc. **Closes B2.2 Gap 1; B2.4 still pending for Gap 2** (per-role `catalog_published_at` annotation). |
| 2026-05-13 | **B11.2.b test fake clock fix merged** (PR #604 → `97720187`). Housekeeping closure of the cross-lane finding escalated by Codex's C-5 worker (PR #600) and recorded in Bundle 42's change-log. **Root cause: hypothesis (c) — a real B11.2.b regression that PR #586's audit missed.** Investigation agent (task `abe2185e0b6b14891`) diagnosed: `_RecordingStepUpGateway` test fake at `test/proxy/b11_2_b_step_up_wiring_test.dart:444+` compared seeded `expiresAt` to real wall-clock UTC via `DateTime.now().toUtc()`, while the rest of the test gate ran on an injected `now` clock. Seeded `expiresAt` for `CHAL_PASSWORD` was `2026-05-13T10:10Z`; once real-world UTC crossed 10:10Z, the fake's `consume` returned null (treating as expired) → router fell through to reject → test failed. **Pure time-bomb pattern**: test passed at PR #586 CI time (authored before 10:10Z), began failing once real-world UTC crossed 10:10Z. **Production code is correct and unchanged** — `auth_step_up_gate.dart` + `auth_step_up_routes.dart` remain canonical on master. Fix is test-only: constructor injection of `DateTime Function() now` into `_RecordingStepUpGateway` with field `final DateTime Function() _now;`; both wall-clock sites in `consume` + `emit` replaced with `_now()`; all 8 instantiation sites updated to pass `now: () => DateTime.utc(2026, 5, 13, 10)` matching the clock threaded into `runStepUpGate`. 1 file / +15/-11. Pre-fix `+7 -1`; post-fix `+8 -0`. `dart analyze` clean. Delegated to Claude lane sub-agent in worktree isolation per `feedback_delegate_fixes_to_agent.md`; agent committed + pushed + opened PR + stopped per agent-led-slices contract. Audit doc: `docs/_audits/post_codex_wave/pr_604_b11_2_b_test_fake_clock_fix_audit.md`. **Pattern lesson for closeout doc-audit**: PR #586 audit should have flagged seeded-`expiresAt`/`DateTime.now()` mismatch in test fakes — same class as PR #588 admin_cors_bootstrap_test drift, but caused by time. Worth a one-line addition to the test-fake checklist. |
| 2026-05-13 | **C-5 merged** (PR #600 → `161e6b85`). Codex mobile-to-Operator-Web handoff bridge. Mobile pointer rows mint B11.1 short-TTL opaque handoff codes via `HandoffCodeClient`, then launch `https://app.forgeflow.app/handoff?code=...&nav=...` via `url_launcher`. Operator Web `/handoff` route parser maps URL/nav/returned-path to stable shell nav IDs; redeems via body-only `POST /v1/auth/handoff/redeem` with `body: {'code': code}` (NEVER `?code=` query string) + current ID token in HTTP header. **CLAUDE.md addendum A1 fully satisfied**: JWTs and Step-Up-Challenge-Id NOT in URL — handoff code is the bridge primitive itself (B11.1's whole purpose). Defense-in-depth: redeem requires `idTokenProvider()` to return non-null/non-empty token, throwing `no_id_token` 401 otherwise — handoff code alone is insufficient. Mobile fallback to clipboard for offline / proxy-5xx / launchUrl failure with plain-English snackbar copy. Missing/expired/rejected handoff fails closed on dedicated landing surface. New `OperatorWebHandoffRedeemGateway` (119 LoC) + `HandoffCodeGateway` mobile wrapper (71 LoC) + `/handoff` router (294 LoC in `operator_web_router.dart`) + mobile pointer row launch logic (+118 in `settings_pointer_row.dart`). 11 files / +878/-64. 51 disclosed tests pass (mint/launch/fallback + `/handoff` parser + redeem + missing/expired/rejected). Pattern B exemplary (worker 14L + executor 14L with file:line citations). No `db/migrations/**`, no `tool/advisor_proxy/**`, no `lib/auth/**`, no `lib/data/**` touched. Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_600_c_5_mobile_handoff_deeplink_audit.md`. **Cross-lane finding escalated**: Codex worker disclosed pre-existing failing test on master at `test/proxy/b11_2_b_step_up_wiring_test.dart:175` ("valid presented Step-Up-Challenge-Id HEADER admits the request" — expected false, actual true). NOT introduced by C-5; Codex respected ownership boundary. Investigation agent spawned (task `abe2185e0b6b14891`) to diagnose root cause; will produce written diagnosis distinguishing test-side drift (admin_cors_bootstrap_test pattern from PR #588) vs. production regression. **Closes mobile-to-Operator-Web bridge for V1.** |
| 2026-05-13 | **C-1a merged** (PR #599 → `755648bf`). Claude lane prep migration that unblocks C-1. Adds `email_event.provider_event_id text` (NULLABLE) + partial `UNIQUE INDEX email_event_provider_event_id_unique ON email_event(provider_event_id) WHERE provider_event_id IS NOT NULL`. Pure additive expand: `ADD COLUMN IF NOT EXISTS` + `CREATE UNIQUE INDEX IF NOT EXISTS` + `COMMENT ON COLUMN`. Zero `DROP`/`ALTER COLUMN TYPE`/`CREATE POLICY`/`GRANT`/`REVOKE`. Idempotent DDL; `ACCESS EXCLUSIVE` held microseconds (NULLABLE `ADD COLUMN` is metadata-only in PG 11+). RLS posture unchanged — existing FK-join regime (`email_event_per_tenant_select` via EXISTS join through `email_outbox.operator_id`) stays. 7 files / +389/-11. 14 test cases pass (pin column type, NULLABLE, partial UNIQUE predicate is load-bearing, RLS unchanged, no new GRANT). Pattern B exemplary (worker 14L + executor 14L; executor adds additive-expand-discipline + partial-UNIQUE-predicate-correctness + RLS-posture-preservation lenses). Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_599_c_1a_email_event_provider_id_audit.md`. **Honest disclosures** (non-blocking): (a) pre-existing B2.1 runbook count drift (41 → 43) recovered as scope-adjacent doc hygiene; (b) C-1's second gap (`email_credentials.event_webhook_pubkey_pem` missing for ECDSA pubkey resolution) NOT addressed — worker flagged that a **C-1b** slice may be needed if C-1's worker hits that path. **Migration queued for staging + Production1 apply at slot 43** (per `runbooks/phase_9_production1_migration_apply_runbook.md`). **Unblocks C-1**. |
| 2026-05-13 | **L_A1 + L_A2 + C-1a ledger rows added** per operator's 2026-05-13 Path A picks for B8 and C-1. **B8 Path A**: promote Inheritance Tree primitive (`L_A1`) + descendant-set cache (`L_A2`) from "spec assumption" to actual Lane A ledger rows. Foundations for any hierarchy-aware feature (B8 audit log filter, future region rollups, hierarchy-aware permissions, hierarchy-aware audit aggregates). B8's dependency column updated `—` → `L_A2 merged`. **C-1 Path A**: prep migration (`C-1a`) adds `UNIQUE INDEX` on `email_event(sg_event_id)` so the C-1 receiver can rely on Postgres-enforced dedupe rather than soft-dedupe partial index. Per operator: ship now while there's zero SendGrid traffic, so the first live operator's first email lands against an already-indexed table. C-1's dependency column updated `—` → `C-1a merged`. All three new rows are Claude-owned, operator-gate (schema-touching migrations). Counts: Total 51 → 54; Claude 27 → 30; Operator-gate 31 → 34. Auto-merge unchanged at 20. |
| 2026-05-13 | **B10.2 merged** (PR #594 → `bf09a77f`). Codex vendor applicability admin editor + operator-web wage authority binding. **UI + operator-web binding on top of B10.1 backend (PR #576, Bundle 33).** Pure UI work — no schema/proxy/auth surface change. Admin page with per-`setting_kind` tabs (wage/covers/polling) + vendor table with enable toggle + effective dates + metadata JSON. Operator-web Data Accuracy wage authority picker now live-bound: reads `vendor_applicability` filtered by `setting_kind = 'wage' AND enabled = true AND effective_until IS NULL`; disables vendor source with operator-readable copy when no current wage vendor enabled ("No current wage vendor is enabled for this location yet. Use the manual mix until F&F enables one"). `super_admin` write gate at `admin_routes.dart:1216` matches B10.1's `kFfVendorApplicabilityAdminRoles`. Required `admin_reason` + `Idempotency-Key` (minted per-action) threaded through gateway commands per B10.1 contract. Temporal end/replace only — screen exposes only `gateway.end()` + `gateway.upsert()`; no delete path. 11 files / +1876/-38. 46 disclosed tests pass. Pattern B exemplary (worker 14L + executor 14L). Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_594_b10_2_vendor_applicability_admin_audit.md`. **Closes B10.1's operator-web binding deferral.** No advisor_proxy.dart touch (master post-Bundle 38 at 19,803 / 19,900, headroom 97). |
| 2026-05-13 | **B2.3 + B2.4 ledger rows added** per operator's 2026-05-13 "no business live yet so mutations are fine" direction. Both rows queue Claude lane work to close the two honest forward-looking gaps disclosed by B2.2 (PR #590). **B2.3**: add `GET /v1/admin/auth/role-catalogs/blast-radius?version_id=...` returning `{operator_count, location_count, user_count}` so the publish dialog renders the slice-specced "Affecting N businesses, N locations, N users" numbers. **B2.4**: project `catalog_published_at` onto each `is_seeded = true` role row in `WebTeamRolesGateway`; update operator-web annotation to "Updated by F&F on <date>". Both Small/Low/auto-gate (read-only admin observability + operator-web read-only surface). Rationale per operator: blast radius is zero pre-live, so the safety guardrails the slices add can ship aggressively now — better to land them before first live operator than after. Counts: Total 49 → 51; Claude 25 → 27. |
| 2026-05-13 | **C-4 merged** (PR #592 → `f573f520`). Codex master Demo → Live switch — closes the 4th HP #2 reader-side carve-out specced by ledger row 81. **Doctrine expansion (3 → 4 carve-outs)** documented in `CLAUDE.md` (+2 LoC, file path + rationale) + `docs/contracts/demo_mode_contract.md` (+39/-5: header "two" → "four" rewrite, full Carve-out #4 section). Carve-out #4 is RUNTIME-based (reads `DemoModeStateNotifier.snapshot.hasDemoCategories`) — distinct from #1-3 compile-time `kDemoMode` branches. **No `kDemoMode` branch, no `demo_*` table, no reader repository fork.** New `DemoModeStateRepository` (408 LoC) wraps flip (`UPDATE demo_mode_state SET is_demo=false ... RETURNING`) + audit (`demo_mode.master_switch_to_live` via `_auditLogsRepository.writeRow` INSERT-only) + `proxy_requests` idempotency completion in one tenant transaction. New proxy route `POST /v1/operators/{operator_id}/locations/{location_id}/demo-mode-master-switch` with two refusal classes (`live_to_demo_refused` 409 + `demo_mode_already_live` 409). Auth posture mirrors B11.1/B11.2.b precedent verbatim: `kOperatorWriteRoles` (operator owner/admin) role gate → `_operatorLocationScopeAllowed` scope check → Idempotency-Key required + 200-char cap → tenant tx wraps flip + audit + idempotency completion. 15 files / +1963/-63. 39 disclosed tests pass. Pattern B exemplary (worker 14L + executor 14L). Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_592_c_4_demo_live_master_switch_audit.md`. **Bleed-stop ceiling raised 19,700 → 19,900** in same bundle (Option A precedent from Bundles 33/34; C-4 added +120 to `advisor_proxy.dart` — necessary dispatch envelope; master post-merge 19,803, headroom 97). No migration (reuses existing `demo_mode_state` + `proxy_requests`). |
| 2026-05-13 | **B2.2 merged** (PR #590 → `66568b9d`). Claude lane Default Role catalog admin editor UI on top of B2.1 backend (PR #584, Bundle 34). Three-region admin page (current/history/draft) + 5-state double-confirm publish dialog with type-confirm-by-version + operator-web "Default" badge with "Managed by Forge & Flow" annotation on `is_seeded = true` rows. **Pure UI-only — ZERO backend files modified.** F&F admin chrome reused VERBATIM from `feature_flags_admin_screen.dart` (`AppColors.backgroundDeep`, `AdminButtonStyles.primary/secondary/danger`, `display28`/`body13` typography). `super_admin` write gate at `admin_routes.dart:652` matches B2.1's `kDefaultRoleCatalogAdminWriteRoles`. Idempotency-Key minter (UTC microseconds + monotonic counter) at `main_admin.dart:419`. 10 files / +2692/-5. 15 new tests + 21 pre-existing roles_screen tests + 25 admin_shell tests pass. Pattern B exemplary (worker 14L + executor 14L; executor adds honest-gap-discipline + chrome-reuse lenses). Operator approved 2026-05-13 via break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_590_b2_2_default_role_catalog_admin_editor_audit.md`. **Two honest forward-looking gaps disclosed** (worker self-disclosure + executor independent grep verified): (a) numeric blast-radius counts downgraded to plain-English consequence copy (no fabricated numbers) — B2.1 gateway exposes ZERO operator/location/user counts; (b) per-role "Updated by F&F on <date>" downgraded to "Managed by Forge & Flow" — `WebTeamRolesGateway` doesn't carry `catalog_published_at` per role row. Both gaps map to future backend slices: **B2.3** (`GET /v1/admin/auth/role-catalogs/blast-radius?version_id=...` returning `{operator_count, location_count, user_count}`) + **B2.4** (project `catalog_published_at` onto `is_seeded = true` role rows in operator-web gateway). "Seeded" → "Default" rename in operator-web is scope-adjacent but justified per `project_ux_writing_standard.md`. `advisor_proxy.dart` unchanged at 19,688 / 19,700 (UI work doesn't touch monolith). |
| 2026-05-13 | **admin_cors_bootstrap_test fix merged** (PR #588 → `85dbe9e0`). Housekeeping closure of the Bundle 33 deferral ("`admin_cors_bootstrap_test.dart` 5 pre-existing failures needing separate investigation"). Background investigation agent (task `a2d2f5ac88d28ae26`) corrected the misframing: **actual count was 1 failing test**, not 5 — the "5" referred to 5 stacked `expect` calls inside one test case where the first failure halts the test. **Root cause is NOT B10.1.** The failure has existed since commit `0dde9314` on 2026-05-07 (`fix(code-health.ff-policy-fold): feature_flags sentinel operator_id`) which intentionally rewrote the CORS feature-flag SQL from `operator_id IS NULL` to `operator_id = public.feature_flag_system_wide_operator_id()` per `phase_9_scalability_decisions_2026-04-27.md` item 4 (RLS predicates must fold into the operator-leading partial unique index `feature_flags_operator_scope_idx`). Source-grep snapshot in the test never got updated to mirror the new shape; only surfaced 6 days later when PR #581 (A3.4)'s worker happened to re-run this test surface. Fix is a 1-line snapshot update at `test/proxy/admin_cors_bootstrap_test.dart:160` plus a comment block citing commit + migration + phase 9 anchor. 1 file / +16/-3. Pre-fix `flutter test test/proxy/admin_cors_bootstrap_test.dart` → `+21 -1`; post-fix → `+22 -0`. `dart analyze` clean. Production code in `tool/advisor_proxy/proxy_bootstrap.dart` (sentinel SQL at line 8798) **UNTOUCHED** — production contract on master remains canonical per phase 9 item 4. Delegated to a Claude lane sub-agent in worktree isolation per `feedback_delegate_fixes_to_agent.md`; agent committed + pushed + opened PR + stopped per agent-led-slices contract. Triple-safeguard verified (oid → is-ancestor ANCESTOR_OK → `feature_flag_system_wide_operator_id` substring present in `test/proxy/admin_cors_bootstrap_test.dart` on master at line 171). Audit doc: `docs/_audits/post_codex_wave/pr_588_admin_cors_bootstrap_test_fix_audit.md`. **Closes Bundle 33's only outstanding follow-up.** |
| 2026-05-13 | **test/proxy stale-snapshot fix merged** (PR #610 → `2f5de648`). Housekeeping closure of the 2 stale-snapshot failures (3 test cases across 2 files) surfaced by PR #603's full `test/proxy/` sweep. Investigation agent reframed B2.3's worker's "5 test failures in `test/proxy/`" disclosure (Bundle 33-style inflation — test files counted, not cases): actual picture is **2 stale snapshots, 0 wall-clock time bombs, 0 real regressions, 0 unknown**. Fix #1 at `test/proxy/audit_chain_anchors_routes_test.dart:216`: B1 sign-in contract commit `c3f1ce0d` (2026-05-12) intentionally accepts scope-less `super_admin`/`ff_support` → 200 (contract-correct acceptance branch). Test role switched `super_admin` → `operator_owner` to preserve 403 reject-path coverage exactly. Fix #2 at `test/proxy/registry_proxy_health_check_store_test.dart:217+:239`: A3.3 commit `bb88f82b` (2026-05-13 02:03 UTC) narrowed catch site at `tool/advisor_proxy/advisor_proxy.dart:5605` from `catch (_)` to `on Exception catch (_)` (inline rationale on master: "Narrowed to `Exception` so genuine `Error`s ... keep propagating instead of being silently masked behind a green health envelope"). `StateError extends Error` → escapes uncaught. Test fakes switched `StateError(...)` → `Exception(...)` at both sites. **Production code in both root causes is contract-correct and unchanged on master** — `tool/advisor_proxy/advisor_proxy.dart:5605` shows `} on Exception catch (_) {` (independent grep). 3 files / +62/-3 (2 test files + 1 audit doc — investigation report authored in this PR because file didn't exist on master). Combined two-file pre-fix `+21 -3` → post-fix `+24 -0` (net +3 cases recovered, 0 regressions). One residual same-class failure at `test/proxy/auth_location_integrations_route_test.dart:476` (same A3.3 root cause, different production seam) reproduces on master pre-PR — out of scope per brief, filed for separate orchestrator follow-up. Delegated to Claude lane sub-agent in worktree isolation per `feedback_delegate_fixes_to_agent.md`; agent committed + pushed + opened PR + stopped per agent-led-slices contract. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_610_test_proxy_stale_snapshot_fix_audit.md`. Investigation doc: `docs/_audits/post_codex_wave/test_proxy_5_failures_investigation.md`. **Doctrine signals for closeout retro**: (a) count failing test CASES, not files (Bundle 33 inflation pattern recurrent); (b) A3.3 verification command list should have included `registry_proxy_health_check_store_test.dart` — A3.4 checklist should cover every test that hits touched catch sites. |
| 2026-05-13 | **L_A1 merged** (PR #608 → `ed91d90c`). Claude lane Inheritance Tree primitive — foundation for B6/B8/C-6/B2.4 family. **Value class** `InheritanceTreeNode` at `lib/domain/models/inheritance_tree_node.dart` (189 LoC, pure Dart, no Flutter, no `package:postgres`) with open `metadata` slot for forward-compat (L_A2 cache projections + B6/B8 effective-value fields without forcing a value-class break). **Shared visualization widget** `InheritanceTree` at `lib/widgets/inheritance_tree.dart` (320 LoC) — **VISUALIZATION, not selector** — caller-supplied `annotationBuilder` decides effective-value semantics; widget is effective-value-blind with zero mutate affordances. Existing scope selector at `lib/operator_web/widgets/org_unit_tree_view.dart` UNCHANGED. **Three new operator-scoped read methods** on existing `OrgUnitsRepository extends OperatorScopedRepository`: `getOrgUnitTreeForOperator` (2 `withTenant` reads in one transaction → O(N+M) Dart stitch), `getDescendantLocations` (single ltree predicate `org_unit_path <@ scope.path` backed by existing `locations_org_unit_path_gist_idx` GIST index from `db/migrations/202604290101_phase_9_hierarchy_access_wiring.sql`), `getNodeForLocation` (single-leaf shortcut). 5 files / +1,901/-0. 25/25 new tests pass (10 widget + 15 repo). Pattern B exemplary (worker 14L + executor 14L + 3 EXEC+ lenses: selector-vs-visualization discipline / depth-math reconciliation / soft-deleted-ancestor predicate consistency). **Operator decision resolved** — `[operator-approval-required]` prefix flagged the no-migration question (ledger row 48 said "descendant_path denormalization that L_A2 will cache; migration is additive expand" but PR shipped no migration). Worker's rationale (executor-verified): existing `locations.org_unit_path` GIST index from Phase 9 already supports the descendant query L_A2 will cache; L_A2 is a performance denormalization off the same ltree shape, not a structural prereq. Operator picked Option 1 (approve + merge as-is, no migration needed). **Trailing risk acknowledged**: if L_A2's worker later determines a precomputed table is required for performance, L_A1b queued at that point. `tool/advisor_proxy/**` UNTOUCHED (bleed-stop 19,805 / 19,900 headroom 95 — identical to pre-PR). `lib/auth/permission_keys.dart` UNTOUCHED. `audit_logs_update_lint`, `postgres_import_lint`, `dart analyze --fatal-infos` all clean. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_608_l_a1_inheritance_tree_primitive_audit.md`. **Orchestrator note for L_A2 brief**: anchor on existing GIST shape, frame caching as performance projection (not structural prereq). **Unblocks B6, B8, C-6 immediately.** |
| 2026-05-13 | **B2.4 merged** (PR #609 → `a786157d`). Claude lane per-role `catalog_published_at` projection on operator-web roles screen — closes the second of two honest gaps disclosed by B2.2 worker (PR #590). Pattern mirrors B2.3 (Gap 1) closure exactly. Repository `listVisibleRoles` extended with two LEFT JOINs (`public.operators` for pinned-version pointer + `public.default_role_catalog_versions` for metadata); both joins gate on `r.is_seeded = true` so custom rows never inherit catalog metadata. Resolution rule mirrors existing `resolveDefaultRoleCatalogPayload`: pinned → unpinned `is_current = true` → genesis NULL. `RoleRecord`, `TeamRoleCatalogEntry`, `RoleEntry`, `_roleEntry` each gain two nullable fields (`catalogVersionId`, `catalogPublishedAt`); `_teamRoleToJson` adds 2 nullable response keys (additive — mobile + legacy clients tolerate). `roles_screen.dart` seeded-row annotation calls new pure helper `defaultAnnotationCopyForCatalogPublishedAt(publishedAt)` returning "Updated by F&F on Mon D, YYYY" for non-null `DateTime` or fallback "Managed by Forge & Flow" verbatim for null. Defensive parsing surfaces `malformed_response` envelope on ISO-8601 parse failure. **B2.1 grant compatibility verified** — partial UNIQUE INDEX on `default_role_catalog_versions (is_current) WHERE is_current = true` guarantees single-row LEFT JOIN match; pinned-branch is single-row by PK. No row-duplication risk. 10 files / +993/-37. 31/31 new tests pass (5 repo + 5 gateway + 3 screen + 1 regression pin updated); 57-test regression sweep across 7 touched files clean. Pattern B exemplary (worker 14L + executor 14L + 3 EXEC+ lenses: B2.1 grant compatibility / multi-row LEFT JOIN safety / honest-gap discipline). No migration (ledger row 73 explicitly said "no migration expected"). No new permission key (existing `team.roles.view` gate covers read path). `lib/auth/**` UNTOUCHED. `advisor_proxy.dart` +7 LoC response-shape only (bleed-stop 19,805 → 19,812, headroom 88) — pre-reconciled to auto-gate by ledger row 73 (operator-web read-only surface). Auto-gate per ledger row 73; operator approved via expanded break-time delegation. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_609_b2_4_catalog_published_at_projection_audit.md`. **Closes B2.2 Gap 2; B2.x slice family complete** (Gap 1 closed by B2.3 PR #603, Gap 2 closed here). |
| 2026-05-13 | **C-1 merged** (PR #611 → `c3724902`). Claude lane SendGrid Event Webhook receiver — consumes C-1a's prep migration (PR #599) verbatim. **Pre-decode ECDSA P-256 signature verification** (`pointycastle`-backed) over canonical `timestamp \|\| raw_body` byte sequence; verifier runs BEFORE `jsonDecode` (verified at lines 539/721 → 563/740; pinning test "verifier receives the raw body bytes UNCHANGED"). **Pubkey loaded from `SENDGRID_EVENT_WEBHOOK_PUBKEY_PEM` env var only** (matches C-1 spec's preferred source per `c_1_ecdsa_pubkey_gap_investigation.md`; mirrors `EMAIL_CREDENTIALS_ENVELOPE_KEY` precedent; loader invoked per-request so rotated key picks up without proxy restart). **Postgres-enforced dedupe** via `ON CONFLICT (provider_event_id) WHERE provider_event_id IS NOT NULL DO NOTHING` — predicate exactly matches C-1a's partial UNIQUE INDEX. Replayed batches no-op returning 204. Status codes: 204 success (incl. all-duplicate), 401 `bad_signature` on header/verifier failure, 400 `malformed_json`/`malformed_event`, 413 on >1 MB body, 503 `pubkey_not_configured` when env unset (correct SendGrid retry posture — 5xx retries upstream while operators wire the secret). **Route mounted via `main.dart` pre-check ahead of `routeRequest`** — mirrors `adminEmailRouter` + `Phase80IntegrationRoutes` precedent. **`tool/advisor_proxy/advisor_proxy.dart` literally UNTOUCHED** (bleed-stop 19,805 / 19,900 headroom 95 — identical to pre-PR; independent diff returns 0 lines). **`withSystem` admin-pool write path** with stable audit reason `'email_event.insert_provider_event'` — correct posture per CLAUDE.md "RLS-Ready Schema" (inbound webhook has no tenant context). Existing FK-join RLS on `email_event` reads (via `email_outbox.operator_id`) preserved unchanged. 7 files / +2,045/-0. 68/68 tests pass (23 new + 45 sibling regression). Pattern B exemplary (worker 14L + executor 14L + 5 EXEC+ lenses: verify-before-decode discipline / ON CONFLICT predicate match / env-var-only + 503 retry posture / pre-check mount pattern / honest design-decision disclosures). No new migration. No new permission key (`lib/auth/**` UNTOUCHED — ECDSA signature is only gate; no Firebase JWT). `audit_logs_update_lint` clean. **Three honest design disclosures** (non-blocking, forward-looking): (a) `email_credentials.sendgrid_event_webhook_pubkey_pem` column deferred to conditional C-1b (env-var path is V1 launch posture per Hard Promise #7 — "F&F holds all provider keys server-side"); (b) no `audit_logs` writes by design (`email_event` row IS the audit trail; per-webhook-event audit would balloon hash chain 3-5x with zero added signal); (c) pubkey loader called per-request for rotation-without-restart. Auto-gate per ledger row 83 (pre-reconciled by operator's "no business live yet" direction despite proxy-touching surface). Triple-safeguard verified (`kSendGridPubKeyEnvVar = 'SENDGRID_EVENT_WEBHOOK_PUBKEY_PEM'` substring confirmed on master post-merge). Audit doc: `docs/_audits/post_codex_wave/pr_611_c_1_sendgrid_events_webhook_audit.md`. **Operator action for deploy**: wire `SENDGRID_EVENT_WEBHOOK_PUBKEY_PEM` in Cloud Run env. **Unblocks C-2** (wire-or-delete 6 template-only emails). |
| 2026-05-13 | **L_A2 merged** (PR #616 → `f24a5b73`). Claude lane Inheritance descendant-set cache — performance projection over L_A1's `OrgUnitsRepository.getDescendantLocations`. **Option α (app-level memoization) picked** — matches operator's L_A1 merge framing exactly ("anchor on existing GIST shape, frame caching as performance projection (not structural prereq)"). No migration, no new RLS policy, no schema change. New `InheritanceDescendantCache` value class at `lib/services/hierarchy/inheritance_descendant_cache.dart` (316 LoC) with bounded TTL (60s default) + LRU eviction + injectable clock + explicit `invalidate()` seam. **HP #4 cross-tenant isolation pinned in cache key shape**: `InheritanceDescendantCacheKey` requires `operatorId` (line 66); equality (line 81) + hashCode (line 86) both include `operatorId`; cross-tenant cache hit impossible by construction. Pure Dart — no Flutter, no `package:postgres` (verified by inline disclaimer at line 41 + independent grep + `postgres_import_lint`). Cache calls `OrgUnitsRepository.getDescendantLocations` which routes through existing `withTenant` — no bare `current_setting()` reads added. Consumer API `getDescendantLocationsCached({operatorId, locationId, scopeOrgUnitId?, userId?})` mirrors `OrgUnitsRepository.getDescendantLocations` signature for drop-in swap by B6/B8 future consumers. Default OFF (`enabled: false`) so other repo tests stay deterministic; production wiring is OUT OF SCOPE for L_A2 (a future slice opts in via `proxy_bootstrap.dart`). 2 files / +1,188/-0. 17/17 cache tests pass (7 groups: hit/miss, TTL, cross-tenant, invalidate, empty-scope, null-scope, loader-failure, LRU, key equality) + 35/35 L_A1 regression (15 repo + 20 live-binding). Pattern B (worker 14L in PR body + executor 14L + 4 EXEC+ lenses in audit doc: cache-key cross-tenant isolation / loader failure containment / default-OFF discipline / consumer API drop-in compatibility). **Five forward-looking honest disclosures** (non-blocking): (a) per-pod posture (`invalidate()` is local-only; multi-pod needs shorter TTL or cross-pod fanout — deferred to V1+1); (b) no call-site wiring (B6/B8 will own `invalidate()` calls on hierarchy writes); (c) no observability surface yet (`statsSnapshot()` exposed but not projected to `/health` — future slice); (d) successful empty lists ARE cached (zero-locations-under-scope is a real read shape, pinned by test); (e) default OFF discipline (future production wiring slice opts in explicitly). Bleed-stop lint UNCHANGED at 19,812 / 19,900 (headroom 88 — `tool/advisor_proxy/` literally 0-line diff). `lib/auth/**` UNTOUCHED. Operator-gate per ledger row 49; operator approved via expanded break-time delegation; matches operator's L_A1 merge framing verbatim. Triple-safeguard verified (`class InheritanceDescendantCacheKey` symbol confirmed on master post-merge). Audit doc: `docs/_audits/post_codex_wave/pr_616_l_a2_inheritance_descendant_cache_audit.md`. **Unblocks B6 (Codex), B8 (Claude), C-6 (Codex).** |
| 2026-05-13 | **C-2 merged** (PR #617 → `1657d42c`). Claude lane wire-or-delete decision matrix for the 6 template-only emails — **shipped as a stop-and-disclose decision matrix per the worker contract's Block 3 clause**, not as direct wires. Worker exercised the disclose-and-stop authority for 5 of 7 drafts because the emitter call-sites require fundamental architectural decisions the operator should make (not paper-over with speculative wires that would lock the operator into a default they may not want). **Two drafts already-resolved**: A1 (`operator_admin_invite` deleted by A2.2 PR #540), A2 (`operator_invite_first_admin` preserved as admin SendGrid test-connection fixture) + B (deferred to code-health wave per addendum B4). **Five drafts stop-and-disclosed** with per-draft architectural-decision rationale + sizing estimates in `docs/_decisions/c_2_email_template_wire_or_delete_decisions.md`: **C** (`mfa_factor_changed_notice` — no clean server-side emission site, enrollment is client-side, removal already emits to inbox); **D** (`vendor_sync_error_alert` — no production sustained-failure aggregator, per-row email would spam on transients); **E** (`vendor_webhook_signature_alert` — distributed across 20+ per-vendor verifier files, no centralized counter); **F** (`vendor_connection_auto_disabled` — trigger site IS clean at `oauth_refresh_worker:1196` + `:1226` but Cloud Run worker has no fanout dependency wired in `WorkerRuntime`; **RECOMMEND ship soon** via Path (b) ~400 LoC outbox enqueue mirror, OR Path (a) ~600 LoC full fanout); **G** (`tos_version_updated_notice` — no production INSERT into `tos_versions`, TOS publish workflow not yet shipped). 2 files / +219/-10: renderer.dart per-id doc-string extension (every WIRED/TEMPLATE-ONLY constant now carries V1 status + architectural-decision citation) + new decision matrix doc. Pattern B (worker 14L + executor 14L + 3 EXEC+ lenses in audit doc: stop-and-disclose discipline / matrix-on-master vs hold-open / no-speculative-wires). 92/92 email-suite tests pass + 14/14 C-1 sendgrid_events_webhook regression. `tool/advisor_proxy/` 0-line diff. `lib/auth/permission_keys.dart` UNTOUCHED. No migration. No `audit_logs` writes. **Scope-reframed merge** — slice intent satisfied via matrix, actual per-draft wire/delete actions split into 5 follow-up PRs against operator picks (no rework risk). Operator approved via expanded break-time delegation; reframing authorized by worker contract Block 3 explicitly. Triple-safeguard verified. Audit doc: `docs/_audits/post_codex_wave/pr_617_c_2_wire_or_delete_decision_matrix_audit.md`. **⚠ Five architectural decisions queued on operator's plate** (C/D/E/F/G picks) — each unblocks a separate follow-up wire/delete PR. **C-11 (Pressure-test inventory) conditionally unblocked** depending on whether operator wants to lock wire decisions before C-11 reads the inventory. |
| 2026-05-13 | **C-2 operator picks recorded + C-7a approved** (orchestrator housekeeping). Operator returned from break and picked from C-2 matrix in plain-English shorthand: "keep 2fa, pos connection and vendor connection only" → translated to per-draft picks: **C (`mfa_factor_changed_notice`) WIRE**, **D (`vendor_sync_error_alert`) WIRE**, **E (`vendor_webhook_signature_alert`) DELETE**, **F (`vendor_connection_auto_disabled`) WIRE**, **G (`tos_version_updated_notice`) DELETE**. Plus operator's earlier "yes to all" pre-approved the **C-7a prep migration** unblocking Codex's C-7 (additive `ADD COLUMN IF NOT EXISTS mfa_factors.recovery_codes_viewed_at timestamptz NULL`; mirrors C-1a → C-1 pattern). **Worker source for follow-ups**: parallel Claude lane session per operator direction (this orchestrator session does NOT spawn workers for these follow-ups; we only record picks on master). Five new ledger rows added: `C-2-C` (WIRE mfa, ~250-500 LoC, sub-path TBD at spawn), `C-2-D` (WIRE vendor_sync with first-failure-of-outage detector, ~400-700 LoC), `C-2-F` (WIRE auto_disabled via Path (b) outbox enqueue ~400 LoC per worker recommendation), `C-2-Del` (DELETE E + G templates + renderer constants, bundled ~150 LoC delete net; mirrors A2.2 PR #540 shape), `C-7a` (DB column migration, additive expand, idempotent). Counts: Total 54 → 59; Claude 30 → 35. Decision matrix doc updated with operator-picks section at `docs/_decisions/c_2_email_template_wire_or_delete_decisions.md`. **C-11 (Pressure-test inventory) decision pending separately** — can proceed against the matrix as it stands, or wait for the 4 follow-up slice merges. Migration queue: C-7a joins staging + Production1 apply queue at slot 44 once merged. |
